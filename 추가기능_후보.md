# 추가 구현 가능 기능 후보

> 작성일: 2026-03-23 | 최종 수정: 2026-03-23
> 기준: 현재 Spring Boot + React + SQLite 스택에서 구현 가능한 기능만 포함
> 총 기능 수: 36개

---

## 🔴 높은 임팩트

### 1. 이메일 알림
**설명**: DANGER 탐지 시 설정한 이메일 주소로 경고 메일 자동 발송

**사용 흐름**:
```
랜섬웨어 탐지 → Spring Boot SMTP → 사용자 이메일 수신
```

**구현 범위**:
- `spring-boot-starter-mail` 의존성 추가
- `EmailNotificationService` 구현 (탐지 시 자동 호출)
- 설정 페이지에 이메일 주소 입력 UI 추가
- 이메일 템플릿 작성 (탐지 일시, 파일명, 위험도 포함)

**환경변수**: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD`

---

### 2. 자동 파일 격리
**설명**: DANGER 판정 시 의심 파일을 자동으로 격리 폴더로 이동하여 랜섬웨어 확산 차단

**사용 흐름**:
```
DANGER 탐지 → 해당 파일 → /격리폴더/날짜/파일명 으로 이동
대시보드에서 격리된 파일 목록 확인 + 복원 가능
```

**구현 범위**:
- `QuarantineService` 구현 (파일 이동 + DB 기록)
- 격리 폴더 경로 설정 UI
- 격리 파일 목록 페이지 (복원 버튼 포함)
- 자동 격리 ON/OFF 토글 설정

**주의**: 정상 파일 오탐 시 복원 경로 반드시 제공해야 함

---

### 3. 수동 스캔
**설명**: 특정 폴더를 즉시 스캔하여 현재 상태를 분석하는 온디맨드 기능

**사용 흐름**:
```
폴더 선택 → 스캔 시작 버튼 → 진행률 표시 → 결과 리포트
```

**구현 범위**:
- `ScanService` 구현 (기존 `FileCollectorService` 재활용)
- 스캔 결과 임시 저장 및 조회 API
- 프론트엔드 스캔 진행률 UI (현재 `ScanApi.js` 기반 구현 시작됨)
- 스캔 결과 요약 (파일 수, 의심 파일 수, 고엔트로피 파일 목록)

---

## 🟡 중간 임팩트

### 4. 탐지 리포트 PDF 생성
**설명**: 기간별 탐지 현황을 PDF 문서로 내보내기

**사용 흐름**:
```
기간 선택 → PDF 생성 버튼 → 다운로드
```

**구현 범위**:
- `Apache PDFBox` 또는 `iText` 라이브러리 추가
- `ReportService` 구현 (날짜 범위별 알림·로그 집계)
- PDF 레이아웃: 요약 테이블, 탐지 건수 차트, 상세 목록
- 백엔드 `GET /api/report?from=&to=&format=pdf` API

---

### 5. 감시 폴더별 탐지 현황 분리
**설명**: 여러 감시 폴더가 있을 때 폴더별로 탐지 현황을 분리하여 표시

**사용 흐름**:
```
대시보드 → 폴더별 탭 전환 → 각 폴더의 탐지 건수·로그 확인
```

**구현 범위**:
- 로그·알림 DB에 `watchedFolder` 컬럼 추가 (또는 파일 경로에서 파싱)
- 프론트엔드 폴더별 필터 UI
- 폴더별 탐지 건수 집계 API

---

### 6. 위험 파일 히스토리 추적
**설명**: 동일 파일에 대해 시간순으로 이벤트 변화를 추적하여 랜섬웨어 진행 과정 시각화

**사용 흐름**:
```
의심 파일 클릭 → 해당 파일의 이벤트 타임라인 확인
(CREATE → MODIFY × 3 → 엔트로피 급등 → DELETE)
```

**구현 범위**:
- 로그에서 동일 파일 경로 이벤트 그룹핑 API
- 프론트엔드 타임라인 컴포넌트

---

### 7. 알림 심각도별 소리 알림
**설명**: DANGER 탐지 시 경고음 재생 (브라우저 Web Audio API 활용)

**사용 흐름**:
```
DANGER 판정 → 브라우저에서 경고음 재생 + 화면 강조 표시
```

**구현 범위**:
- 프론트엔드 `AudioAlert` 컴포넌트
- 대시보드 폴링(또는 SSE)으로 새 DANGER 알림 감지 시 트리거
- 설정 페이지에 소리 알림 ON/OFF 토글

---

## 🟢 낮은 임팩트 (완성도 향상)

### 8. 감시 중단 / 재개 버튼
**설명**: 일시적으로 파일 감시를 멈추고 재개하는 토글 버튼

**구현 범위**:
- `WatcherService`에 pause/resume 메서드 추가
- `GET /api/watcher/status`, `POST /api/watcher/pause`, `POST /api/watcher/resume` API
- 대시보드에 상태 표시 + 버튼

---

### 9. 로그 검색 및 필터
**설명**: 이벤트 로그 페이지에서 파일명, 이벤트 타입, 날짜 범위로 필터링

**구현 범위**:
- 백엔드 `LogRepository`에 동적 쿼리 추가
- 프론트엔드 검색바 + 필터 드롭다운 UI

---

### 10. 탐지 임계값 UI 설정
**설명**: `application.yml`의 분석 파라미터(엔트로피 임계값, 윈도우 크기 등)를 웹 UI에서 조정 가능하게 함

**구현 범위**:
- 설정 페이지에 슬라이더/입력 UI 추가
- `SettingsService`에 분석 파라미터 저장·조회 추가
- `EventWindowAggregator`에서 DB 설정값 우선 참조

---

---

## 🔴 높은 임팩트 (추가)

### 11. Slack / Teams 웹훅 알림
**설명**: DANGER 탐지 시 Slack 또는 Microsoft Teams 채널로 알림 전송

**사용 흐름**:
```
DANGER 탐지 → Webhook URL로 HTTP POST → Slack/Teams 채널에 메시지 표시
```

**구현 범위**:
- `WebhookNotificationService` 구현 (RestTemplate으로 POST 전송)
- 설정 페이지에 Webhook URL 입력 필드 추가
- 메시지 포맷: 탐지 일시, 파일명, 위험도, Gemini 대응 가이드 요약

---

### 12. 실시간 대시보드 업데이트 (SSE)
**설명**: 현재 대시보드는 새로고침해야 최신 정보가 반영됨. SSE(Server-Sent Events)로 탐지 발생 시 자동으로 화면 갱신

**사용 흐름**:
```
랜섬웨어 탐지 → 서버가 SSE로 푸시 → 브라우저 자동 갱신 (새로고침 없이)
```

**구현 범위**:
- `SseEmitter` 기반 `AlertStreamController` 구현
- `NotificationService`에서 탐지 시 SSE 이벤트 발행
- 프론트엔드 `EventSource` 연결 + 대시보드 자동 갱신

---

### 13. 자동 백업 트리거
**설명**: DANGER 탐지 시 감시 중인 폴더를 지정한 백업 경로로 즉시 복사

**사용 흐름**:
```
DANGER 탐지 → 감시 폴더 전체 → /백업경로/날짜시간/ 으로 복사
```

**구현 범위**:
- `BackupService` 구현 (Java NIO 파일 복사)
- 설정 페이지에 백업 경로 지정 UI
- 자동 백업 ON/OFF 토글
- 백업 이력 조회 페이지

---

## 🟡 중간 임팩트 (추가)

### 14. 탐지 민감도 슬라이더
**설명**: 전문 지식 없이도 탐지 민감도를 낮음/중간/높음으로 조정 가능

**사용 흐름**:
```
설정 → 민감도 슬라이더 조정 → 내부적으로 엔트로피 임계값·윈도우 크기 자동 계산
```

**구현 범위**:
- 민감도 단계(1~5)를 `application.yml` 파라미터 조합으로 매핑
- 설정 페이지에 슬라이더 UI 추가
- `SettingsService`에 민감도 저장·적용 로직

---

### 15. 파일 엔트로피 시각화
**설명**: 감시 중인 파일들의 엔트로피 분포를 히트맵 또는 차트로 시각화

**사용 흐름**:
```
대시보드 → 엔트로피 탭 → 파일별 엔트로피 색상 표시 (낮음=파랑, 높음=빨강)
```

**구현 범위**:
- `FileSnapshotStore`에서 현재 엔트로피 값 조회 API
- 프론트엔드 `recharts` 기반 히트맵 또는 산점도 차트

---

### 16. 감시 스케줄 설정
**설명**: 특정 시간대에만 감시를 활성화 (예: 업무 시간 외에만 감시)

**사용 흐름**:
```
설정 → 감시 시작 시간 / 종료 시간 지정 → 해당 시간에만 WatcherService 활성화
```

**구현 범위**:
- `@Scheduled`로 시간대 체크 후 WatcherService pause/resume
- 설정 페이지에 시간 범위 입력 UI
- 현재 감시 활성 여부 대시보드에 표시

---

### 17. 랜섬웨어 패밀리 상세 정보
**설명**: 탐지된 랜섬웨어 패밀리 이름 클릭 시 해당 패밀리의 공격 방식·특징 정보 표시

**사용 흐름**:
```
알림 상세 → "LockBit" 클릭 → 패밀리 설명, 주요 타깃, 알려진 확장자 등 표시
```

**구현 범위**:
- 패밀리별 정보 JSON 파일 내장 (LockBit, WannaCry, REvil 등 주요 10개)
- `FamilyInfoController` API
- 프론트엔드 모달 또는 사이드패널

---

### 18. 탐지 트렌드 차트 (시간별)
**설명**: 시간대별·요일별 탐지 건수 추이를 라인 차트로 표시

**사용 흐름**:
```
통계 페이지 → 시간별 / 일별 / 주별 탭 전환 → 탐지 건수 추이 차트
```

**구현 범위**:
- `NotificationRepository`에 날짜 범위·그룹핑 쿼리 추가
- 프론트엔드 `recharts` LineChart 컴포넌트

---

## 🟢 낮은 임팩트 (추가)

### 19. 다크모드
**설명**: 라이트/다크 테마 토글

**구현 범위**:
- CSS 변수 기반 테마 전환
- `localStorage`에 테마 설정 저장
- 헤더에 토글 버튼 추가

---

### 20. AI 서버 연결 상태 표시
**설명**: 대시보드에 AI 서버 온라인/오프라인 상태 표시

**구현 범위**:
- `GET /api/status/ai` → AI 서버 `/health` 호출 후 결과 반환
- 대시보드 우측 상단에 상태 인디케이터 (녹색/빨간색)

---

### 21. 알림 읽음 / 안읽음 상태
**설명**: 알림 목록에서 읽은 것과 안읽은 것 구분, 안읽은 알림 수 배지 표시

**구현 범위**:
- `notification` 테이블에 `is_read` 컬럼 추가
- `PATCH /api/notifications/:id/read` API
- 사이드바 메뉴에 안읽은 알림 수 배지

---

### 22. 파일 복구 단계별 가이드
**설명**: 랜섬웨어 탐지 후 Gemini 가이드와 별개로, 일반 사용자를 위한 단계별 복구 체크리스트 제공

**사용 흐름**:
```
DANGER 알림 → "복구 가이드 보기" 버튼 → 단계별 체크리스트 모달
(1. 네트워크 차단 → 2. 격리 확인 → 3. 백업 복원 → 4. 백신 검사 ...)
```

**구현 범위**:
- 프론트엔드 단계별 체크리스트 모달 컴포넌트 (백엔드 불필요)
- 체크 완료 항목 localStorage 저장

---

### 23. 감시 폴더 디스크 사용량 표시
**설명**: 감시 중인 폴더의 현재 디스크 사용량과 파일 수 표시

**구현 범위**:
- `Files.walk()`로 폴더 크기·파일 수 계산 API
- 설정 페이지 감시 폴더 목록에 사용량 표시

---

---

## 🔴 높은 임팩트 (추가 2)

### 24. 오탐 신고 (False Positive 마킹)
**설명**: 알림 상세 페이지에서 "오탐입니다" 버튼 클릭 시 해당 파일/경로를 예외 규칙에 자동 추가

**사용 흐름**:
```
알림 상세 → "오탐 신고" 버튼 → 해당 파일 경로 예외 규칙 자동 등록 → 이후 동일 경로 알림 미발생
```

**구현 범위**:
- `NotificationController`에 `PATCH /api/notifications/:id/false-positive` API 추가
- `SettingsService.addException()` 자동 호출
- 알림 상세 페이지에 버튼 추가

---

### 25. 사용자 정의 탐지 규칙
**설명**: "X초 안에 Y개 파일이 변경되면 알림" 같은 커스텀 규칙을 UI에서 직접 설정

**사용 흐름**:
```
설정 → 커스텀 규칙 추가 → 이벤트 수 / 시간 윈도우 / 파일 타입 조건 설정
```

**구현 범위**:
- `custom_rule` DB 테이블 추가
- `EventWindowAggregator`에서 커스텀 규칙 조회 후 조건 체크
- 규칙 CRUD UI

---

### 26. 라이브 이벤트 피드
**설명**: 파일 이벤트가 발생할 때마다 실시간으로 스크롤되는 터미널 스타일 피드

**사용 흐름**:
```
대시보드 → 라이브 피드 탭 → CREATE / MODIFY / DELETE 이벤트 실시간 스크롤
```

**구현 범위**:
- SSE 기반 이벤트 스트림 `GET /api/events/stream`
- 프론트엔드 자동 스크롤 로그 컴포넌트

---

## 🟡 중간 임팩트 (추가 2)

### 27. 탐지 히트맵 (시간 × 요일)
**설명**: 어느 시간대·요일에 탐지가 집중되는지 히트맵으로 시각화

**사용 흐름**:
```
통계 페이지 → 히트맵 탭 → 월~일 × 0~23시 격자, 탐지 건수에 따라 색상 강도
```

**구현 범위**:
- `notification` 테이블에서 시간대·요일별 GROUP BY 집계 API
- 프론트엔드 CSS Grid 기반 히트맵 컴포넌트

---

### 28. 알림에 노트 작성
**설명**: 특정 알림에 조사 메모를 남길 수 있는 기능 ("확인 완료", "오탐으로 판단" 등)

**사용 흐름**:
```
알림 상세 → 노트 입력창 → 저장 → 알림 목록에 노트 아이콘 표시
```

**구현 범위**:
- `notification` 테이블에 `note` 컬럼 추가
- `PATCH /api/notifications/:id/note` API
- 알림 상세 페이지에 textarea + 저장 버튼

---

### 29. 보안 점수 (Security Score)
**설명**: 현재 설정 상태 + 최근 탐지 현황을 종합한 보안 점수(0~100)를 대시보드에 표시

**점수 계산 기준 예시**:
```
감시 폴더 설정됨       +20점
예외 규칙 적절히 설정  +10점
최근 7일 DANGER 없음  +30점
최근 7일 WARNING 없음 +20점
로그 정상 수집 중      +20점
```

**구현 범위**:
- `SecurityScoreService` 구현 (각 항목 체크 후 합산)
- 대시보드에 점수 게이지 UI

---

### 30. 파일 확장자 분포 차트
**설명**: 탐지된 의심 이벤트에서 어떤 확장자가 가장 많이 나타나는지 파이/바 차트로 표시

**사용 흐름**:
```
통계 페이지 → 확장자 분포 탭 → 상위 10개 확장자 차트
```

**구현 범위**:
- 로그 데이터에서 확장자 추출·집계 API
- 프론트엔드 `recharts` PieChart / BarChart

---

### 31. 배치 알림 삭제
**설명**: 알림 목록에서 여러 개를 체크박스로 선택 후 한 번에 삭제

**구현 범위**:
- `DELETE /api/notifications` 요청 바디에 `ids` 배열로 일괄 삭제 API
- 프론트엔드 체크박스 + 선택 삭제 버튼

---

### 32. 자동 로그 정리
**설명**: N일 이상 지난 로그를 자동으로 삭제하는 주기 작업

**사용 흐름**:
```
설정 → 로그 보관 기간 설정 (7일 / 30일 / 90일 / 무제한)
→ 매일 자정 자동 삭제
```

**구현 범위**:
- `@Scheduled` 기반 `LogCleanupService` 구현
- `LogRepository`에 날짜 기준 삭제 쿼리 추가
- 설정 페이지에 보관 기간 드롭다운

---

## 🟢 낮은 임팩트 (추가 2)

### 33. 북마크 알림
**설명**: 중요한 알림을 북마크하여 별도 탭에서 모아 볼 수 있는 기능

**구현 범위**:
- `notification` 테이블에 `is_bookmarked` 컬럼 추가
- `PATCH /api/notifications/:id/bookmark` API
- 알림 목록에 북마크 필터 탭

---

### 34. 다국어 지원 (한국어 / 영어)
**설명**: UI 언어를 한국어/영어로 전환

**구현 범위**:
- `react-i18next` 라이브러리 적용
- 한국어 / 영어 번역 JSON 파일 작성
- 헤더에 언어 전환 버튼

---

### 35. 키보드 단축키
**설명**: 자주 쓰는 동작에 단축키 지정

**예시 단축키**:
```
R → 새로고침
D → 대시보드로 이동
N → 알림 페이지로 이동
L → 로그 페이지로 이동
/ → 검색창 포커스
```

**구현 범위**:
- `useKeyboardShortcut` 커스텀 훅 구현
- 단축키 안내 모달 (`?` 키로 표시)

---

### 36. 감시 폴더 추가 마법사
**설명**: 감시 폴더를 처음 추가할 때 단계별 안내로 권장 설정을 함께 제안

**사용 흐름**:
```
1단계: 폴더 선택
2단계: 중요도 선택 (중요 문서 / 일반 / 시스템)
3단계: 권장 예외 규칙 자동 제안 (임시 파일, 캐시 폴더 등)
4단계: 완료
```

**구현 범위**:
- 프론트엔드 멀티스텝 폼 컴포넌트
- 중요도별 권장 예외 규칙 프리셋 JSON

---

## 우선순위 추천

| 순위 | 기능 | 임팩트 | 구현 난이도 |
|------|------|--------|------------|
| 1 | 수동 스캔 | 높음 | 낮음 — `ScanApi.js` 이미 존재 |
| 2 | 이메일 알림 | 높음 | 낮음 — Spring Boot SMTP 표준 기능 |
| 3 | 실시간 대시보드 (SSE) | 높음 | 중간 — SseEmitter 구현 필요 |
| 4 | Slack/Teams 웹훅 | 높음 | 낮음 — HTTP POST 하나로 구현 |
| 5 | 자동 파일 격리 | 높음 | 중간 — 능동적 방어, 차별점 |
| 6 | 탐지 리포트 PDF | 중간 | 중간 — 기업·기관 사용자 필요 |
| 7 | 알림 읽음 상태 | 중간 | 낮음 — DB 컬럼 하나 추가 |
| 8 | AI 서버 연결 상태 | 중간 | 낮음 — 완성도 향상 |
| 9 | 감시 중단/재개 | 중간 | 낮음 — API 2개 추가 |
| 10 | 다크모드 | 낮음 | 낮음 — CSS 변수만 추가 |
