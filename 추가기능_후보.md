# 추가 구현 가능 기능 후보

> 기준: Spring Boot + React + SQLite 현재 스택에서 구현 가능한 기능
> 총 기능 수: 59개 (🔴 높음 23 / 🟡 중간 23 / 🟢 낮음 13)
> 최종 수정: 2026-03-30

---

## 🔴 높은 임팩트 (23개)

| # | 기능명 | 설명 | 핵심 구현 | 난이도 |
|---|--------|------|-----------|--------|
| 1 | 이메일 알림 | DANGER 탐지 시 설정한 이메일 주소로 경고 메일 자동 발송 | `spring-boot-starter-mail` + `EmailNotificationService`. 환경변수: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD` | 낮 |
| 2 | 자동 파일 격리 | DANGER 판정 시 의심 파일을 격리 폴더로 이동하여 확산 차단. 대시보드에서 복원 가능 | `QuarantineService` (파일 이동 + DB 기록) + 격리 파일 목록 페이지 + 자동 격리 ON/OFF 설정 | 중 |
| 3 | 수동 스캔 | 특정 폴더를 즉시 전수 스캔하여 현재 상태 분석. 진행률 표시 후 결과 리포트 | `ScanService` (`FileCollectorService` 재활용). `ScanApi.js` 이미 일부 구현됨 | 낮 |
| 4 | Slack / Teams 웹훅 알림 | DANGER 탐지 시 Slack 또는 Microsoft Teams 채널로 메시지 자동 전송 | `WebhookNotificationService` (`RestTemplate` POST) + 설정 페이지 Webhook URL 입력 필드 | 낮 |
| 5 | 실시간 대시보드 업데이트 (SSE) | 탐지 발생 시 새로고침 없이 대시보드 자동 갱신 | `SseEmitter` 기반 `AlertStreamController` + `NotificationService`에서 이벤트 발행 + 프론트 `EventSource` | 중 |
| 6 | 자동 백업 트리거 | DANGER 탐지 시 감시 폴더 전체를 지정 백업 경로로 즉시 복사 | `BackupService` (Java NIO 파일 복사) + 백업 경로 설정 UI + 백업 이력 조회 페이지 | 중 |
| 7 | 오탐 신고 (False Positive 마킹) | 알림 상세에서 "오탐 신고" 클릭 시 해당 파일 경로를 예외 규칙에 자동 등록 | `PATCH /api/notifications/:id/false-positive` + `SettingsService.addException()` 자동 호출 | 낮 |
| 8 | 사용자 정의 탐지 규칙 | "X초 안에 Y개 파일이 변경되면 알림" 같은 커스텀 조건을 UI에서 직접 설정 | `custom_rule` DB 테이블 + `EventWindowAggregator`에서 커스텀 규칙 조건 체크 + 규칙 CRUD UI | 높 |
| 9 | 라이브 이벤트 피드 | CREATE / MODIFY / DELETE 이벤트가 발생할 때마다 실시간 스크롤되는 터미널 스타일 피드 | SSE 기반 `GET /api/events/stream` + 프론트엔드 자동 스크롤 로그 컴포넌트 | 중 |
| 10 | 긴급 격리 모드 (Emergency Lock) | DANGER 탐지 또는 수동으로 감시 폴더 내 모든 파일을 개별 읽기 전용으로 전환하여 기존 파일 암호화 즉시 차단. 새 파일 생성은 막지 않고 기존 파일 수정만 봉쇄 | `EmergencyLockService` — `Files.walk()`로 감시 폴더 내 파일 순회 후 파일별 읽기 전용 설정 (Windows: `Files.setAttribute("dos:readonly", true)` / macOS·Linux: `Files.setPosixFilePermissions()`로 쓰기 비트 제거). OS 분기 처리 필요. `POST/DELETE /api/watcher/emergency-lock` + 대시보드 긴급 격리 버튼 | 중 |
| 11 | 탐지 알림 에스컬레이션 | DANGER 알림이 N분 내 읽음 처리되지 않으면 이메일·웹훅 재발송으로 반복 알림 | `@Scheduled` 기반 `EscalationCheckService` + `notification.escalation_count` 컬럼 + 에스컬레이션 간격 설정 | 중 |
| 12 | 아웃바운드 웹훅 (Generic Webhook) | 탐지 발생 시 임의 URL로 POST 요청 발송 — SIEM, 티켓팅, 사내 채널 연동. HMAC-SHA256 서명 포함 | `WebhookService` + `webhook` DB 테이블 (url, secret, enabled) + 설정 페이지 URL 등록·테스트 UI | 중 |
| 13 | 감시 시스템 헬스 패널 | 감시 동작 여부·마지막 이벤트 시간·분당 이벤트 수·AI 서버 응답 시간을 대시보드에 실시간 표시 | `HealthStatusService` + `GET /api/health/status` + 대시보드 헬스 패널 컴포넌트 | 낮 |
| 14 | 탐지 억제 규칙 (Suppression) | 야간 백업·백신 스캔 등 특정 경로·시간대에서 발생하는 알림 자동 억제 | `suppression_rule` DB 테이블 (path_pattern, start_time, end_time, days_of_week) + `EventWindowAggregator`에서 체크 | 중 |
| 15 | 탐지 전후 파일 변경 타임라인 | 알림 상세에서 탐지 발생 전후 30초 동안의 파일 이벤트를 색상 구분 타임라인으로 시각화 | `GET /api/notifications/:id/timeline` (탐지 ±30초 로그 조회) + 프론트엔드 수평 타임라인 컴포넌트 | 낮 |
| 16 | 파일 SHA-256 해시 변조 감지 | 파일의 SHA-256 해시를 스냅샷에 저장하고 MODIFY 이벤트 시 해시가 완전히 바뀌면 암호화 확정 신호로 처리. 엔트로피 방식보다 더 확실한 변조 탐지 | `FileCollectorService`에 `MessageDigest.getInstance("SHA-256")` 추가. `FileSnapshot`에 `hash` 필드 추가. 해시 변경 시 `fileEncryptLikeCount` 증가 | 중 |
| 17 | 인시던트 보고서 자동 생성 | DANGER 알림 기반으로 CERT/SOC 제출용 인시던트 보고서 초안 자동 생성 (공격 타임라인, 영향 파일 목록, 대응 조치 포함) | 이미 연동된 Gemini API에 보고서 전용 프롬프트 추가. `IncidentReportController` + 알림 상세 페이지 "보고서 생성" 버튼 + PDF 다운로드 | 낮 |
| 18 | AI 모델 핫 리로드 | 관리자 UI에서 새 모델 파일(`model_xgb.json`, `features.json`)을 업로드하면 AI 서버 재시작 없이 즉시 교체 | `POST /api/admin/model/upload` (Spring Boot) → AI 서버 `POST /admin/reload` 호출. FastAPI에서 전역 모델 변수 갱신 + 이전 모델 백업 | 중 |
| 19 | Electron 긴급 전체 화면 경보 | DANGER 탐지 시 Electron 앱이 전체 화면 긴급 알림 창을 강제 표시하여 즉각 조치 요구. 백그라운드 감시 중에도 팝업 | `main.js`에서 DANGER 상태 폴링. 감지 시 새 `BrowserWindow` 전체 화면으로 `emergency.html` 표시 + 소리 재생 | 낮 |
| 20 | 피해 범위 자동 추산 | 탐지 알림에 영향 파일 수·암호화 의심 파일 수·예상 피해 크기(MB) 자동 계산하여 포함 | `EventWindowAggregator` 윈도우 집계 시 영향 파일 크기 합산. `notification` 테이블에 `estimatedDamageMb`, `encryptSuspectCount` 컬럼 추가 | 낮 |
| 21 | 탐지 공격 세션 그룹화 | 짧은 시간(5분) 내 연속 발생한 DANGER/WARNING 알림들을 하나의 공격 세션으로 묶어 표시. 수십 개의 알림이 따로따로 나오는 문제 해결 | `AttackSessionService` — 인접 알림 시간 차이 < 5분이면 동일 `attackSessionId`로 묶음. 대시보드에 세션 단위 요약 카드 | 중 |
| 22 | VirusTotal 해시 조회 연동 | 탐지된 파일의 SHA-256 해시를 VirusTotal Public API에 조회하여 알려진 악성코드 여부 확인. 결과를 알림 상세에 표시 | `VirusTotalService` — `GET https://www.virustotal.com/api/v3/files/{hash}`. 무료 API Key로 분당 4회 조회. 비동기 처리 + API Key 환경변수 | 낮 |
| 23 | 감시 폴더 삭제 · 이름 변경 감지 | 감시 중인 루트 폴더 자체가 삭제되거나 이름이 변경될 때 즉시 DANGER 경보 발생. 랜섬웨어가 폴더 구조를 파괴할 때 탐지 | `WatcherService`에서 감시 폴더의 **상위 디렉토리**도 추가 감시. 루트 폴더 DELETE 이벤트 탐지 시 긴급 알림 트리거 + 감시 재등록 시도 | 낮 |

---

## 🟡 중간 임팩트 (23개)

| # | 기능명 | 설명 | 핵심 구현 | 난이도 |
|---|--------|------|-----------|--------|
| 24 | 탐지 리포트 PDF 생성 | 기간별 탐지 현황(요약 테이블·차트·상세 목록)을 PDF로 내보내기 | `Apache PDFBox` 또는 `iText` + `ReportService` + `GET /api/report?from=&to=&format=pdf` | 중 |
| 25 | 감시 폴더별 탐지 현황 분리 | 여러 감시 폴더가 있을 때 폴더별로 탐지 건수·로그를 분리하여 탭으로 표시 | 로그·알림 DB에 `watchedFolder` 컬럼 추가 + 폴더별 집계 API + 프론트엔드 탭 UI | 중 |
| 26 | 위험 파일 히스토리 추적 | 동일 파일의 이벤트 변화를 시간순 타임라인으로 시각화 (CREATE→MODIFY×3→엔트로피 급등→DELETE) | 로그에서 동일 파일 경로 이벤트 그룹핑 API + 프론트엔드 타임라인 컴포넌트 | 낮 |
| 27 | 알림 심각도별 소리 알림 | DANGER 탐지 시 브라우저에서 경고음 재생 + 화면 강조 표시 | 프론트엔드 `AudioAlert` 컴포넌트 + 대시보드 폴링(또는 SSE)으로 새 DANGER 감지 시 트리거 + 소리 ON/OFF 설정 | 낮 |
| 28 | 탐지 민감도 슬라이더 | 전문 지식 없이 탐지 민감도를 낮음/중간/높음으로 조정. 내부적으로 엔트로피 임계값·윈도우 크기 자동 매핑 | 민감도 1~5 단계 → `application.yml` 파라미터 조합 매핑 + `SettingsService` + `EventWindowAggregator` DB 설정 참조 | 중 |
| 29 | 파일 엔트로피 시각화 | 감시 중인 파일들의 엔트로피 분포를 히트맵 또는 산점도 차트로 표시 (낮음=파랑, 높음=빨강) | `FileSnapshotStore` 현재 엔트로피 조회 API + 프론트엔드 `recharts` 히트맵 / 산점도 | 낮 |
| 30 | 감시 스케줄 설정 | 특정 시간대에만 감시 활성화 (예: 업무 시간 외에만) | `@Scheduled`로 시간대 체크 후 `WatcherService` pause/resume + 설정 페이지 시간 범위 입력 UI | 중 |
| 31 | 랜섬웨어 패밀리 상세 정보 | 탐지된 패밀리명(LockBit 등) 클릭 시 공격 방식·주요 타깃·알려진 확장자 정보 표시 | 패밀리별 정보 JSON 파일 내장 (주요 10개) + `FamilyInfoController` API + 프론트엔드 모달 | 낮 |
| 32 | 탐지 트렌드 차트 (시간별) | 시간대별·일별·주별 탐지 건수 추이를 라인 차트로 표시 | `NotificationRepository` 날짜·그룹핑 쿼리 + 프론트엔드 `recharts` LineChart | 낮 |
| 33 | 탐지 히트맵 (시간 × 요일) | 어느 시간대·요일에 탐지가 집중되는지 히트맵으로 시각화 (월~일 × 0~23시 격자) | `notification` 테이블 시간대·요일별 GROUP BY 집계 API + CSS Grid 기반 히트맵 컴포넌트 | 낮 |
| 34 | 알림에 노트 작성 | 특정 알림에 조사 메모 저장 ("확인 완료", "오탐으로 판단" 등). 알림 목록에 노트 아이콘 표시 | `notification.note` 컬럼 추가 + `PATCH /api/notifications/:id/note` + 알림 상세 textarea | 낮 |
| 35 | 보안 점수 (Security Score) | 설정 상태 + 최근 탐지 현황을 종합한 보안 점수(0~100)를 대시보드에 게이지로 표시 | `SecurityScoreService` (감시 폴더 설정·최근 DANGER 없음·로그 수집 등 항목 합산) + 대시보드 게이지 UI | 낮 |
| 36 | 파일 확장자 분포 차트 | 탐지된 의심 이벤트에서 가장 많이 등장한 확장자 상위 10개를 파이/바 차트로 표시 | 로그에서 확장자 추출·집계 API + 프론트엔드 `recharts` PieChart / BarChart | 낮 |
| 37 | 배치 알림 삭제 | 알림 목록에서 체크박스로 다수 선택 후 한 번에 삭제 | `DELETE /api/notifications` 요청 바디에 `ids` 배열 + 프론트엔드 체크박스 + 선택 삭제 버튼 | 낮 |
| 38 | 자동 로그 정리 | N일 이상 지난 로그를 매일 자정 자동 삭제 (7일 / 30일 / 90일 / 무제한 선택) | `@Scheduled` 기반 `LogCleanupService` + `LogRepository` 날짜 기준 삭제 쿼리 + 설정 보관 기간 드롭다운 | 낮 |
| 39 | 대시보드 시간 범위 선택 | 대시보드 통계·차트를 오늘 / 이번 주 / 이번 달 / 전체 기간으로 필터링 | 각 통계 API에 `?range=today\|week\|month\|all` 파라미터 추가 + 대시보드 기간 선택 컴포넌트 | 낮 |
| 40 | 이번 주 vs 지난 주 비교 | SAFE / WARNING / DANGER 건수를 이번 주와 지난 주 나란히 비교하여 증가율 퍼센트 표시 | `GET /api/notifications/weekly-compare` API + 프론트엔드 `recharts` GroupedBarChart | 낮 |
| 41 | 자주 변경되는 파일 TOP 10 | 감시 기간 동안 가장 많이 이벤트가 발생한 파일 경로 랭킹 (랜섬웨어 집중 공격 대상 파악) | `event_log` 경로별 COUNT GROUP BY ORDER BY 쿼리 + `GET /api/logs/top-files?limit=10` | 낮 |
| 42 | 탐지 결과 재분석 | 기존 로그 항목을 선택하여 현재 AI 모델로 다시 분석 (모델 업데이트 후 과거 데이터 재평가) | `POST /api/logs/:id/reanalyze` + `event_log`에 피처 벡터 JSON 컬럼 저장 (신규 로그부터 적용) | 중 |
| 43 | 알림 필터 프리셋 저장 | 자주 사용하는 알림 필터 조합을 이름 붙여 저장하고 한 클릭으로 재적용 | `filter_preset` DB 테이블 (name, filter_json) + `GET/POST/DELETE /api/filter-presets` + 프리셋 드롭다운 | 낮 |
| 44 | 시스템 리소스 모니터링 | WatcherService의 CPU·메모리 사용량·업타임을 대시보드에 표시 | Spring Boot Actuator `jvm.memory.used`, `process.cpu.usage` 활용 + `GET /api/health/resources` 래핑 | 낮 |
| 45 | CSV 일괄 예외 규칙 등록 | CSV 파일 업로드로 예외 규칙을 한 번에 다수 등록 (대규모 환경 초기 설정) | `POST /api/settings/exceptions/import` (MultipartFile CSV 파싱 + 일괄 저장) + 결과 미리보기 모달 | 낮 |
| 46 | 탐지 알림 카테고리 태그 | 알림에 사용자 정의 태그 부착 ("조사 중", "처리 완료", "오탐 의심"). 태그로 필터링 가능 | `notification_tag` DB 테이블 (notification_id, tag_name) + `POST/DELETE /api/notifications/:id/tags` + 태그 필터 | 낮 |

---

## 🟢 낮은 임팩트 (완성도 향상, 13개)

| # | 기능명 | 설명 | 핵심 구현 | 난이도 |
|---|--------|------|-----------|--------|
| 47 | 감시 중단 / 재개 버튼 | 일시적으로 파일 감시를 멈추고 재개하는 토글 버튼 | `WatcherService`에 pause/resume 메서드 추가 + `POST /api/watcher/pause`, `/resume` + 대시보드 상태 표시 | 낮 |
| 48 | 로그 검색 및 필터 | 이벤트 로그 페이지에서 파일명·이벤트 타입·날짜 범위로 필터링 | `LogRepository` 동적 쿼리 추가 + 프론트엔드 검색바 + 필터 드롭다운 UI | 낮 |
| 49 | 탐지 임계값 UI 설정 | `application.yml`의 엔트로피 임계값·윈도우 크기 등 분석 파라미터를 웹 UI에서 조정 | 설정 페이지 슬라이더/입력 UI + `SettingsService`에 파라미터 저장·조회 + `EventWindowAggregator` DB 설정 우선 참조 | 중 |
| 50 | 다크모드 | 라이트 / 다크 테마 토글 | CSS 변수 기반 테마 전환 + `localStorage` 설정 저장 + 헤더 토글 버튼 | 낮 |
| 51 | AI 서버 연결 상태 표시 | 대시보드에 AI 서버 온라인/오프라인 상태 인디케이터 표시 | `GET /api/status/ai` → AI 서버 `/health` 호출 후 결과 반환 + 대시보드 우측 상단 색상 인디케이터 | 낮 |
| 52 | 알림 읽음 / 안읽음 상태 | 알림 목록에서 읽은 것과 안읽은 것 구분. 사이드바에 안읽은 알림 수 배지 표시 | `notification.is_read` 컬럼 추가 + `PATCH /api/notifications/:id/read` + 사이드바 배지 | 낮 |
| 53 | 파일 복구 단계별 가이드 | DANGER 탐지 후 일반 사용자를 위한 단계별 복구 체크리스트 (네트워크 차단→격리→백업 복원→백신) | 프론트엔드 단계별 체크리스트 모달 (백엔드 불필요) + 체크 완료 항목 localStorage 저장 | 낮 |
| 54 | 감시 폴더 디스크 사용량 표시 | 설정 페이지 감시 폴더 목록에 현재 디스크 사용량과 파일 수 표시 | `Files.walk()`로 폴더 크기·파일 수 계산 API + 설정 페이지 사용량 표시 | 낮 |
| 55 | 북마크 알림 | 중요한 알림을 북마크하여 별도 탭에서 모아볼 수 있는 기능 | `notification.is_bookmarked` 컬럼 추가 + `PATCH /api/notifications/:id/bookmark` + 북마크 필터 탭 | 낮 |
| 56 | 다국어 지원 (한국어 / 영어) | UI 언어를 한국어 / 영어로 전환 | `react-i18next` + 한국어·영어 번역 JSON 파일 + 헤더 언어 전환 버튼 | 중 |
| 57 | 키보드 단축키 | 자주 쓰는 동작에 단축키 지정 (R=새로고침, D=대시보드, N=알림, L=로그, /=검색) | `useKeyboardShortcut` 커스텀 훅 + `?` 키로 단축키 안내 모달 표시 | 낮 |
| 58 | 감시 폴더 추가 마법사 | 폴더 추가 시 단계별 안내로 권장 예외 규칙을 함께 제안 (중요 문서 / 일반 / 시스템) | 프론트엔드 멀티스텝 폼 + 중요도별 권장 예외 규칙 프리셋 JSON | 낮 |
| 59 | 파일 권한 보안 감사 | 감시 폴더 내 파일 중 다른 사용자(others)에게 쓰기·실행 권한이 부여된 파일 목록을 조회하고 권한 수정 기능 제공. 파일 보안 감사 목적 | macOS·Linux: `Files.getPosixFilePermissions()`로 `OTHERS_WRITE`·`OTHERS_EXECUTE` 체크. Windows: JNA(`jna-platform`) 추가 후 SID(`S-1-1-0` Everyone) 직접 비교로 언어 독립적 처리. OS 분기 필수. `Files.walk()` 주기 스캔(`@Scheduled`) + 결과 목록 페이지 + 권한 수정 버튼(`Files.setPosixFilePermissions()`) | 중 |

---

## 우선순위 추천

| 순위 | 기능명 | 임팩트 | 난이도 | 선정 이유 |
|------|--------|--------|--------|-----------|
| 1 | 수동 스캔 (#3) | 🔴 높음 | 낮 | `ScanApi.js` 이미 일부 구현, 완성까지 가장 빠름 |
| 2 | 이메일 알림 (#1) | 🔴 높음 | 낮 | Spring Boot SMTP 표준 기능, 구현 범위 명확 |
| 3 | 오탐 신고 (#7) | 🔴 높음 | 낮 | API 1개 + 버튼 1개, 사용자 불편 즉시 해소 |
| 4 | Electron 긴급 전체 화면 경보 (#19) | 🔴 높음 | 낮 | Electron 이미 있음, `BrowserWindow` + `emergency.html` 추가만 필요 |
| 5 | 인시던트 보고서 자동 생성 (#17) | 🔴 높음 | 낮 | Gemini 이미 연동됨, 프롬프트 변경만으로 구현 가능 |
| 6 | 감시 폴더 삭제·이름 변경 감지 (#23) | 🔴 높음 | 낮 | WatcherService 감시 범위 소폭 확장, 탐지 누락 구멍 차단 |
| 7 | 피해 범위 자동 추산 (#20) | 🔴 높음 | 낮 | 이미 집계하는 데이터를 활용, 컬럼 추가와 합산 로직만 필요 |
| 8 | VirusTotal 해시 조회 (#22) | 🔴 높음 | 낮 | 무료 API, 기존 탐지에 외부 위협 정보 보강 |
| 9 | Slack / Teams 웹훅 (#4) | 🔴 높음 | 낮 | HTTP POST 하나로 구현, 실무 활용도 높음 |
| 10 | 실시간 대시보드 SSE (#5) | 🔴 높음 | 중 | 현재 새로고침 필요한 UX 개선, 탐지 즉시 반응 |
