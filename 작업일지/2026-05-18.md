# 작업일지 — 2026-05-18

---

## 1. macOS DMG 서명 문제 수정 및 재릴리스

### 기존 상태
`electron-builder` 기본 adhoc 서명: `Sealed Resources=none`

### 문제점
다운로드 후 macOS Gatekeeper가 "앱이 수정 또는 손상되었습니다" 오류 표시. `Sealed Resources=none` 상태에서는 우클릭 → 열기 우회도 불가.

### 변경 내용
`afterSign.js` 훅 추가 → `codesign --deep --force --sign -` 전체 번들 재서명. `package.json`에 `"afterSign": "./afterSign.js"` 등록. GitHub Releases에 재업로드. 다운로드 사이트 URL 수정(대시 → 점), macOS 첫 실행 안내 문구도 "우클릭 → 열기"로 업데이트.

---

## 2. 인수테스트 시나리오 업데이트

### 기존 상태
S-01 step 3: "시스템 설정 → 개인 정보 보호 및 보안 → 그래도 열기"

### 문제점
새 DMG는 "개발자를 확인할 수 없습니다" 단계로 개선됨. 시스템 설정 경로 불필요.

### 변경 내용
`문서/인수테스트_시나리오.md` step 3 → "앱 아이콘 우클릭 → 열기"로 수정.

---

## 3. 인수테스트 결과 기반 버그 수정 (4건)

### 이슈 1: 행위 탐지 후 대시보드 미업데이트

#### 기존 상태
```js
useEffect(() => {
  loadSummary(); // 마운트 1회만 호출
}, []);
```

#### 문제점
탐지 발생 시 새로고침 없이는 상태(DANGER/WARNING/SAFE)와 최근 이벤트가 갱신되지 않음.

#### 변경 내용
```js
// loadSummary를 useCallback으로 추출
const loadSummary = useCallback(async () => { ... }, []);

// 마운트 + 5초 폴링
useEffect(() => {
  loadSummary();
  getWatcherStatus().then(data => { if (data?.running) setIsWatching(true); }).catch(()=>{});
}, [loadSummary]);

useEffect(() => {
  const id = setInterval(() => { loadSummary(); refresh(); }, 5000);
  return () => clearInterval(id);
}, [loadSummary, refresh]);
```
`useLogs(5)`에서 `refresh` 추가 destructure.

---

### 이슈 3: 긴급 전체화면 경보 확인 버튼 작동 안 함

#### 기존 상태
```html
<button onclick="window.close()">확인</button>
```
`emergencyWindow`에 preload 없음.

#### 문제점
Electron에서 `contextIsolation: true` + preload 없는 창의 `window.close()`는 차단됨.

#### 변경 내용
- `preload.js`에 `closeEmergencyWindow: () => ipcRenderer.send('close-emergency-window')` 추가
- `main.js`: `emergencyWindow`에 `preload.js` 연결 + `ipcMain.on('close-emergency-window', ...)` 핸들러 추가
- `emergency.html`: `window.electronAPI.closeEmergencyWindow()` 호출로 교체

---

### 이슈 4: 감시/검사 시작 후 다른 페이지 이동 시 버튼 상태 초기화

#### 기존 상태
`isWatching`이 React 로컬 state(`useState(false)`)로만 관리됨.

#### 문제점
페이지를 벗어났다가 대시보드로 돌아오면 컴포넌트가 재마운트되어 `isWatching = false` 초기화 → "감시 시작" 버튼이 다시 표시됨.

#### 변경 내용
백엔드 `GET /watcher/status` 엔드포인트 추가 (`WatcherController.java`).
```java
@GetMapping("/status")
public ResponseEntity<Map<String, Boolean>> status(HttpSession session) {
    return ResponseEntity.ok(Map.of("running", watcherService.isRunning(ownerKey)));
}
```
프론트 `WatcherApi.js`에 `getWatcherStatus()` 추가. 마운트 시 상태 조회 후 `setIsWatching(true)` 복원.

---

### 이슈 5: AI 대응 가이드 동일한 답변

#### 기존 상태
`fallbackGuidance()`가 인자 없이 고정 문자열 반환.

#### 문제점
`GEMINI_API_KEY` 미설정 시 항상 동일한 텍스트 출력. 탐지 내용과 무관한 범용 체크리스트.

#### 변경 내용
`fallbackGuidance(AiPayload, AiResult)`로 시그니처 변경. 탐지 등급·AI 점수·영향 파일 수·감지 활동(암호화·이름변경 등)을 헤더에 포함하고, 암호화/대량 이름변경 감지 시 해당 조치 항목 우선 표시. 비고란에 Gemini API 키 미설정 안내 추가.

---

## 이슈 2: 이메일 알림 작동 안 함 (코드 변경 없음)

`SMTP_USERNAME` / `SMTP_PASSWORD` 환경변수 미설정이 원인. `EmailNotificationService`는 해당 값이 비어 있으면 의도적으로 발송을 건너뜀(정상 동작). 이메일 기능 활성화를 위해서는 서버 실행 전 환경변수 설정 필요:
```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your@gmail.com
SMTP_PASSWORD=앱비밀번호
```

---

## 변경 파일 목록

| 파일 | 변경 내용 |
|------|-----------|
| `watchservice_electron/afterSign.js` | 신규 생성 (adhoc 전체 번들 서명) |
| `watchservice_electron/package.json` | afterSign 훅 등록 |
| `watchservice_electron/preload.js` | `closeEmergencyWindow` IPC 추가 |
| `watchservice_electron/main.js` | emergencyWindow preload 연결 + close IPC 핸들러 |
| `watchservice_electron/assets/emergency.html` | 확인 버튼 IPC 호출로 교체 |
| `watchservice_fe/src/pages/mainboard/MainBoardPage.jsx` | 폴링 + 감시 상태 복원 |
| `watchservice_fe/src/api/WatcherApi.js` | `getWatcherStatus` 추가 |
| `watchservice_be/.../watcher/WatcherController.java` | `GET /watcher/status` 엔드포인트 추가 |
| `watchservice_be/.../ai/GeminiAdviceService.java` | fallback 동적화 |
| `download_site/index.html` | DMG URL 수정, macOS 안내 문구 수정 |
| `문서/인수테스트_시나리오.md` | macOS 첫 실행 절차 수정 |

---

## 4. macOS 재빌드 및 릴리스 배포

### 기존 상태
GitHub Releases v1.0.0에 이전 빌드(`WatchService.Agent-1.0.0-arm64.dmg`) 업로드된 상태.

### 문제점
`./build.sh --electron` 실행 시 `Unsupported class file major version 69` 오류 발생. 시스템 기본 JDK가 Java 25로 변경되어 Gradle Groovy DSL 컴파일러가 Java 25 클래스파일을 파싱 불가.

### 변경 내용
`watchservice_be/gradle.properties`에 `org.gradle.java.home` 설정으로 Gradle 데몬 JVM을 Java 17로 고정.
```properties
org.gradle.java.home=/Users/sanghyeok/Library/Java/JavaVirtualMachines/ms-17.0.16/Contents/Home
```
빌드 후 `gh release upload v1.0.0 WatchService-Agent-1.0.0-arm64.dmg --clobber`로 새 DMG(174MB) 업로드.
`download_site/index.html` macOS 다운로드 URL 파일명 `WatchService.Agent-` → `WatchService-Agent-`, 파일 크기 155MB → 174MB 수정.

---

## 5. macOS "앱이 손상됨" 오류 수정

### 기존 상태
`afterSign.js`: `codesign --deep --force --sign -` 한 번만 실행.

### 문제점
1. JRE 내 Mach-O 바이너리(39개)가 개별 서명 없이 `--deep`으로 묶여 sealed resources 불일치 → Gatekeeper "손상됨" 판정.
2. electron-builder DMG 생성 과정에서 서명이 깨짐 → DMG 내 앱 `code has no resources` 오류.

### 변경 내용
- `afterSign.js`: JRE 내 Mach-O 바이너리 개별 서명 후 `--deep` 적용.
- `afterAllArtifactBuild.js` 신규: 서명된 `mac-arm64` 앱으로 `hdiutil create` 직접 DMG 재생성.
- `download_site/index.html`: xattr 명령어 안내 추가.

---

## 6. 긴급 전체화면 경보 미작동 수정

### 기존 상태
```javascript
dangerPollInterval = setInterval(() => {
  const req = http.get(url, { headers: { Accept: 'application/json' } }, ...);
}, 3000);
```

### 문제점
main.js 폴링이 Node.js `http` 모듈로 쿠키 없이 요청 → BrowserWindow 세션과 다른 `ownerKey` 발급 → 프론트엔드가 저장한 DANGER 알림을 조회 불가. 또한 `page=1` 파라미터가 Spring Pageable 0-indexed 기준으로 2페이지를 조회.

### 변경 내용
```javascript
const cookies = await session.defaultSession.cookies.get({ url: `http://localhost:${PORT}` });
const jsessionid = cookies.find(c => c.name === 'JSESSIONID');
const cookieHeader = jsessionid ? `JSESSIONID=${jsessionid.value}` : '';
// HTTP 요청에 Cookie 헤더 포함, page 파라미터 제거
```
`session` import 추가, 폴링 콜백 async 변환.

---

## 7. 인수테스트 시나리오 재검토 버그 수정 3건

### #2 S-13 — 트레이 동작 (main.js)

#### 기존 상태
트레이 더블클릭만 반응. "감시 시작/중지" 메뉴 없음. 트레이 표시가 감시 상태와 무관.

#### 문제점
단일 클릭으로 창이 열리지 않음. 트레이 컨텍스트 메뉴에 감시 제어 항목 없음. 폴링이 ownerKey 불일치로 감시 상태 반영 불가.

#### 변경 내용
- `isWatching` 전역 변수 추가
- `tray.on('click', ...)` 단일 클릭 → 창 포커스
- `buildContextMenu()`에 "감시 시작/중지" 항목 추가
- 폴링 루프 내 `/watcher/status` 주기적 조회 → `isWatching` 동기화 → 트레이 메뉴 재빌드

---

### #3 S-10 — PDF 리포트 한글 깨짐 (ReportService.java)

#### 기존 상태
```java
String reportFrom = from != null ? from : "전체";
String reportTo   = to   != null ? to   : "전체";
// ...
return s.length() > max ? s.substring(0, max - 1) + "…" : s;
```

#### 문제점
PDFBox `PDType1Font(HELVETICA)`는 ASCII 전용. `"전체"`, `"…"` 포함 시 `IllegalArgumentException` 발생 → PDF 생성 실패.

#### 변경 내용
```java
String reportFrom = from != null ? from : "ALL";
String reportTo   = to   != null ? to   : "ALL";
// ...
return s.length() > max ? s.substring(0, max - 1) + "..." : s;
```

---

### #4 S-11 — 로그 통계 탭 전환 (LogsPage.jsx)

#### 기존 상태
```jsx
<button onClick={() => navigate('/logs/top-files')}>자주 변경 파일 TOP 10</button>
<button onClick={() => navigate('/logs/extension-stats')}>확장자 분포</button>
```

#### 문제점
별도 페이지로 이동 → 뒤로가기 필요. 로그 목록과 통계를 동시에 탐색 불가.

#### 변경 내용
- `LogsPage.jsx`: `activeTab` state 추가. 상단에 "로그 목록 / 자주 변경 파일 TOP 10 / 확장자 분포" 3탭 표시. 탭 클릭 시 컴포넌트 인라인 렌더링.
- `TopFilesPage.jsx`, `ExtensionStatsPage.jsx`: `embedded` prop 추가. `embedded={true}` 시 h1·설명·뒤로가기 버튼 숨김.

---

### S-09 / S-12 시나리오 문서 수정

- `문서/인수테스트_시나리오.md` S-09: "설정 → 알림 설정 → 감시 스케줄 섹션" → "설정 홈 → 감시 스케줄 카드"로 실제 UI 경로 반영
- S-12: "피드백 입력 폼 → 제출" 흐름 제거. 오탐 신고 버튼 클릭 즉시 [FP] 등록되는 실제 동작으로 수정

---

## 변경 파일 목록 (7번 작업)

| 파일 | 변경 내용 |
|------|-----------|
| `watchservice_electron/main.js` | isWatching 전역, 트레이 단일클릭, 감시 메뉴, 상태 동기화 |
| `watchservice_be/.../report/ReportService.java` | "전체"→"ALL", "…"→"..." ASCII 치환 |
| `watchservice_fe/src/pages/logs/LogsPage.jsx` | activeTab 탭 전환 추가, navigate 제거 |
| `watchservice_fe/src/pages/logs/TopFilesPage.jsx` | embedded prop 추가 |
| `watchservice_fe/src/pages/logs/ExtensionStatsPage.jsx` | embedded prop 추가 |
| `문서/인수테스트_시나리오.md` | S-09 경로 수정, S-12 오탐 흐름 수정 |

---

## 8. 이메일 DANGER 알림 미발송 디버그 + Gemini 프롬프트 수정 + 재빌드·릴리스

### 기존 상태
- `NotificationService.java`: `@Slf4j` 어노테이션 없음, 이메일 발송 경로 로그 없음
- `EmailNotificationService.java`: 발송 건너뜀 조건이 `log.debug` (기본 로그 레벨에서 보이지 않음)
- `GeminiAdviceService.buildPrompt()`: 보안 전문가 관점의 전문 용어 위주 프롬프트

### 문제점
- 테스트 이메일은 성공하나 DANGER 탐지 시 실제 알림 이메일이 발송되지 않음
- `ownerKey` 불일치 또는 email 조회 실패 여부를 추적할 로그가 없어 원인 파악 불가
- Gemini 응답이 비전공자에게 어려운 표현으로 구성됨

### 변경 내용
```java
// NotificationService.java — @Slf4j 추가 + DANGER 로그
@Slf4j
public class NotificationService {
    public void saveNotification(Notification notification) {
        notificationRepository.insertNotification(notification);
        if ("DANGER".equals(notification.getAiLabel())) {
            String email = settingsService.getAlertEmail(notification.getOwnerKey());
            log.info("[NotificationService] DANGER 감지 — ownerKey={}, alertEmail='{}'", ...);
            if (email != null && !email.isBlank()) {
                emailNotificationService.sendDangerAlert(email, notification);
            }
        }
    }
}
```
```java
// EmailNotificationService.java — debug → warn
log.warn("[EmailNotificationService] 수신 이메일 미설정 — 발송 건너뜀");
log.warn("[EmailNotificationService] SMTP 계정 미설정 — 발송 건너뜀");
```
`GeminiAdviceService.buildPrompt()`: 비전공자 대상 6개 항목 체크리스트 형식으로 수정 (임의 삭제·몸값 지불 금지, 한국어 간결 표현 명시).

macOS DMG 재빌드 (x64: 184MB, arm64: 173MB), GitHub Releases `WatchService-Agent-1.0.0-arm64.dmg` 교체, 다운로드 사이트 183MB → 173MB 업데이트.

---

## 변경 파일 목록 (8번 작업)

| 파일 | 변경 내용 |
|------|-----------|
| `watchservice_be/.../alerts/NotificationService.java` | @Slf4j 추가, DANGER 감지 로그 추가 |
| `watchservice_be/.../alerts/EmailNotificationService.java` | log.debug → log.warn (발송 건너뜀 조건) |
| `watchservice_be/.../ai/GeminiAdviceService.java` | buildPrompt 비전공자 대상 6항목 체크리스트로 수정 |
| `download_site/index.html` | macOS DMG 파일 크기 183MB → 173MB |

---

## 9. DANGER 이메일 알림 미발송 — 테스트 발송 메커니즘으로 교체

### 기존 상태
```java
// sendDangerAlert: Notification 객체 필드로 buildHtmlBody() 호출
helper.setText(buildHtmlBody(notification), true);

private String buildHtmlBody(Notification n) {
    String createdAt = n.getCreatedAt() != null ? FMT.format(n.getCreatedAt()) : "-";
    // ... n.getAiScore(), n.getTopFamily(), n.getAiDetail() 등 참조
    return "...".formatted(createdAt, score, family, n.getAffectedFilesCount(), ...);
}
```

### 문제점
`buildHtmlBody()` 내부에서 `notification` 필드 접근 중 NPE 또는 기타 예외가 발생해도 `catch (Exception e)`에서 로그만 찍고 삼켜짐. 테스트 이메일은 고정 문자열만 사용하므로 동일한 경로에서 성공하지만 DANGER 알림은 실패.

### 변경 내용
```java
// sendDangerAlert: buildHtmlBody 제거, 테스트 발송과 동일한 고정 HTML 사용
helper.setSubject("[WatchService] ⚠ DANGER — 랜섬웨어 의심 활동 탐지");
helper.setText("""
    <div style="font-family:sans-serif;padding:24px;background:#f8fafc;">
      <h2 style="color:#dc2626;">⚠ DANGER — 랜섬웨어 의심 활동 탐지</h2>
      <p style="font-size:15px;">WatchService Agent가 랜섬웨어 의심 활동을 탐지했습니다.</p>
      <p style="font-size:15px;font-weight:bold;color:#dc2626;">앱을 열어 즉시 확인해주세요.</p>
      <p style="color:#6b7280;font-size:13px;">WatchService Agent 대시보드에서 상세 분석 내용을 확인할 수 있습니다.</p>
    </div>
    """, true);
```
`buildHtmlBody()` 메서드 및 `DateTimeFormatter FMT` 필드 삭제.

---

## 변경 파일 목록 (9번 작업)

| 파일 | 변경 내용 |
|------|-----------|
| `watchservice_be/.../alerts/EmailNotificationService.java` | sendDangerAlert 고정 HTML로 교체, buildHtmlBody·FMT 제거 |
