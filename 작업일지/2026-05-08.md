# 2026-05-08 작업일지

## 작업 내용: Windows 인스톨러 빌드 및 GitHub Release 업로드

### 환경
- macOS → Windows 노트북으로 전환 후 첫 빌드

---

### 변경 파일

| 파일 | 변경 내용 |
|------|---------|
| `watchservice_be/gradle.properties` | `org.gradle.java.home` 제거 (플랫폼 고정 경로 → JAVA_HOME 환경변수 위임) |
| `download_site/index.html` | Windows 다운로드 버튼 활성화 (disabled 제거, .msi 링크 연결) |

---

### 작업 순서

1. **도구 설치 (winget)**
   - Eclipse Temurin JDK 17 (`EclipseAdoptium.Temurin.17.JDK`) — jlink용 jmods 포함
   - GitHub CLI (`GitHub.cli` v2.92.0)

2. **gradle.properties 수정**
   - 기존: `org.gradle.java.home=/Users/sanghyeok/.../ms-17.0.16/Contents/Home` (맥북 경로)
   - 변경: 해당 줄 삭제 → 빌드 시 `JAVA_HOME` 환경변수 사용

3. **인스톨러 빌드**
   - `bash build.sh --electron` (JAVA_HOME 명시적 지정)
   - 트러블슈팅: electron-builder의 `winCodeSign` 추출 시 macOS 심볼릭 링크 권한 오류 (exit code 2)
     - 해결: `winCodeSign-2.6.0` 캐시 디렉토리를 수동 생성 후 빌드 재시도
   - 결과물: `watchservice_electron/dist/WatchService Agent 1.0.0.msi` (162MB)

4. **GitHub Release 업로드**
   - `gh auth login` 으로 GitHub 인증
   - `gh release upload v1.0.0 "WatchService Agent 1.0.0.msi" --clobber`
   - 업로드된 asset 이름: `WatchService.Agent.1.0.0.msi`

5. **다운로드 페이지 업데이트**
   - `download_site/index.html` Windows 버튼 `disabled` → 활성화
   - 링크: `https://github.com/Aikaku/watchservice/releases/latest/download/WatchService.Agent.1.0.0.msi`
   - 커밋 & push → GitHub Pages 자동 반영

---

### 빌드 결과
- ✅ JAR 빌드 성공
- ✅ JRE 번들 생성 (jlink, 47MB)
- ✅ MSI 인스톨러 생성 (162MB)
- ✅ GitHub Release v1.0.0 에 MSI 업로드 완료
- ✅ 다운로드 페이지 Windows 버튼 활성화

---

### 주의사항 (다음 빌드 시)

- Windows에서 빌드 시 `JAVA_HOME`을 JDK 17로 반드시 지정:
  ```bash
  export JAVA_HOME="/c/Program Files/Eclipse Adoptium/jdk-17.0.19.10-hotspot"
  export PATH="$JAVA_HOME/bin:$PATH"
  bash build.sh --electron
  ```
- `winCodeSign` 캐시 (`%LOCALAPPDATA%\electron-builder\Cache\winCodeSign\winCodeSign-2.6.0\`) 가 없으면 다시 심볼릭 링크 오류 발생 → 캐시 수동 생성 필요 (또는 Windows 개발자 모드 활성화)
