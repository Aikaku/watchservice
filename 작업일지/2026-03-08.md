# 2026-03-08 작업 일지

## 작업 내용

### 1. GEMINI_API_KEY 설정

`.env` 파일에 실제 API 키 입력 완료.

- `watchservice_be/.env`: `GEMINI_API_KEY` 실제 값으로 교체

### 2. 피처 계산 개선 구현 확인

이전 세션(2026-03-06)에서 구현된 5가지 피처 개선 사항이 정상 적용되어 있음을 확인.

| 개선 | 파일 | 상태 |
|------|------|------|
| EntropyAnalyzer 3구간 샘플링 | `collector/business/EntropyAnalyzer.java` | ✅ 구현 완료 |
| rename ext-append 패턴 탐지 | `analytics/EventWindowAggregator.java` | ✅ 구현 완료 |
| encrypt_like 절대 엔트로피 탐지 | `analytics/EventWindowAggregator.java` | ✅ 구현 완료 |
| entropy_diff_mean 유의미한 변화만 산입 | `analytics/EventWindowAggregator.java` | ✅ 구현 완료 |
| random_extension_flag 균일 확장자 탐지 | `analytics/EventWindowAggregator.java` | ✅ 구현 완료 |

### 3. Gemini guidance 작동 확인 및 버그 수정

Gemini API 정상 작동 여부를 확인하는 과정에서 3가지 문제 발견 및 수정.

#### 3-1. 테스트 엔드포인트 추가

XGBoost 서버 없이 Gemini만 단독 테스트할 수 있는 엔드포인트 추가.

- **파일**: `ai/AiController.java`
- **엔드포인트**: `GET /ai/gemini/test` — DANGER(LockBit) 시나리오 고정 호출

#### 3-2. bootRun 환경변수 미전달 문제

**원인**: `./start.sh`에서 `.env`를 로드해도 Gradle `bootRun`이 셸 환경변수를 Spring Boot JVM에 전달하지 않음.

**수정**: `build.gradle`에 `bootRun { environment System.getenv() }` 추가.

- **파일**: `build.gradle`

#### 3-3. Gemini API 타임아웃

**원인**: 기본 타임아웃 2500ms가 Gemini 응답 시간(~2.7초)보다 짧아 Read timed out 발생.

**수정**: `application.yml` 기본값 2500 → 10000ms.

- **파일**: `src/main/resources/application.yml`
- **변경**: `gemini.timeout-ms: ${GEMINI_TIMEOUT_MS:10000}`

#### 3-4. Gemini 2.5 Flash thinking 토큰 문제

**원인**: `maxOutputTokens=400`으로 thinking 토큰이 출력 토큰을 소비하여 실제 텍스트가 수 줄에서 잘림.

**수정**:
- `maxOutputTokens` 400 → 1500
- `generationConfig.thinkingConfig.thinkingBudget: 0` 추가 (thinking 비활성화)

- **파일**: `ai/GeminiAdviceService.java`

#### 3-5. start.sh 데몬 재사용 방지

**수정**: `./gradlew bootRun` → `./gradlew bootRun --no-daemon` (키 없이 시작된 기존 데몬 재사용 방지)

- **파일**: `watchservice_be/start.sh`

### 4. CLAUDE.md 규칙 추가

- **파일**: `CLAUDE.md`
- **내용**: `코드/` 폴더 내 파일 수정 절대 금지 규칙 추가 (읽기/참조만 허용)

### 5. 상용화 준비 분석

전체 코드베이스 점검 후 상용화 전 필요 작업 목록 도출.

| 우선순위 | 항목 |
|----------|------|
| P0 | GUI 폴더 선택(`JFileChooser`) → 텍스트 입력으로 교체 (headless 서버 배포 불가) |
| P0 | 관리자 비밀번호 BCrypt 해싱 (현재 평문 비교) |
| P1 | AI 서버 RestTemplate 타임아웃 미설정 (무한 블로킹 위험) |
| P1 | CORS 하드코딩 → 환경변수화 |
| P1 | HTTPS 미설정 |
| P2 | 알림 통계 페이지 미구현 (`fetchAlertStats` 항상 빈 배열) |
| P2 | 프론트 `alert()` 15+ 곳 → Toast/인라인 메시지로 교체 |
| P2 | SQLite WAL 모드 미설정 |
| P2 | 로그 파일 롤링(`logback-spring.xml`) 미설정 |
| P3 | Spring Security 도입 (CSRF, XSS, 세션 보안) |
| P3 | 세션 타임아웃 미설정 |
| P3 | Actuator 메트릭/헬스체크 미구성 |

### 6. JFileChooser 제거 및 백엔드/훅 정리 (P0)

서버에서 JFileChooser Swing 다이얼로그를 직접 띄우는 구조를 제거. headless 서버/Docker/원격 서버 환경 지원.

| 파일 | 변경 내용 |
|------|----------|
| `settings/SettingsController.java` | `pickFolder()` 메서드 및 Swing import(`JFileChooser`, `EventQueue` 등) 전부 제거 |
| `settings/SettingsService.java` | `addWatchedFolder()`에 경로 유효성 검사 추가 — 빈 경로 및 존재하지 않는 디렉토리 등록 시 `IllegalArgumentException` |
| `src/main/resources/application.yml` | `spring.main.headless: false` → `true` |
| `api/SettingApi.js` | `pickFolderPath()`, `pickWatchedFolderPath()` 제거 |
| `hooks/UseWatchedFolders.js` | `promptAndAddFolder` → `addFolder(path, name)` 로 교체, `pickFolderPath` 의존성 제거 |
| `pages/settings/SettingFoldersPage.jsx` | `promptAndAddFolder` → `addFolder` prop 변경 |

### 7. 폴더 선택 UI → 파일시스템 탐색 모달로 교체

텍스트 경로 직접 입력 방식 대신, 서버 파일시스템을 탐색하는 모달 UI로 교체.
"폴더 추가" 버튼 클릭 → 모달 → 디렉토리 탐색 → "이 폴더 선택" 확인 구조.

| 파일 | 변경 내용 |
|------|----------|
| `settings/SettingsController.java` | `GET /settings/folders/browse?path=` 엔드포인트 추가 — 하위 디렉토리 목록 반환, 기본 경로 `user.home`, 숨김 폴더 제외 |
| `api/SettingApi.js` | `browseFolders(path)` 함수 추가 |
| `components/folders/FolderPickerModal.jsx` | 신규 생성 — 서버 파일시스템 탐색 모달. 상위 폴더 이동, 디렉토리 클릭으로 하위 탐색, "이 폴더 선택" 버튼으로 확정 |
| `components/folders/FolderListManager.jsx` | 인라인 텍스트 입력 제거 → `FolderPickerModal` 연동으로 교체 |

### 8. 사용 가이드 페이지 구현

사용자가 조회하고 관리자가 편집할 수 있는 사용 가이드 기능 추가.

| 파일 | 변경 내용 |
|------|----------|
| `admin/GuideRepository.java` | 신규 생성 — `guide` 테이블 생성, 앱 시작 시 기본 가이드 자동 삽입 (INSERT OR IGNORE id=1) |
| `admin/GuideService.java` | 신규 생성 — 가이드 조회/수정 서비스 |
| `admin/GuideController.java` | 신규 생성 — `GET /api/guide` (공개), `PUT /api/admin/guide` (관리자 전용) |
| `api/AdminApi.js` | `fetchGuide()`, `updateGuide(content)` 추가, `put` import 추가 |
| `pages/settings/SettingGuidePage.jsx` | 신규 생성 — 사용자용 가이드 조회 페이지 (`/settings/guide`) |
| `pages/admin/AdminGuidePage.jsx` | 신규 생성 — 관리자용 가이드 편집 페이지 (`/admin/guide`), textarea + 미리보기 + 저장 |
| `pages/settings/SettingHomePage.jsx` | "사용 가이드" 카드 항목 추가 |
| `pages/admin/AdminMainPage.jsx` | "사용 가이드 편집" 링크 추가 |
| `App.js` | `/settings/guide`, `/admin/guide` 라우트 추가 |

## 빌드 결과

```
./gradlew build -x test → BUILD SUCCESSFUL
Gemini 테스트: curl http://localhost:8080/ai/gemini/test → LockBit 대응 가이드 정상 생성 확인
GUI 폴더 선택 제거 후 빌드: BUILD SUCCESSFUL
폴더 선택 모달 교체 후 빌드: BUILD SUCCESSFUL
사용 가이드 페이지 추가 후 빌드: BUILD SUCCESSFUL
```
