# WatchService Agent
## 랜섬웨어 실시간 탐지 시스템

---

## Motivation | 동기

랜섬웨어는 사용자의 파일을 암호화한 뒤 복호화 대가로 금전을 요구하는 악성코드로,
피해가 발생한 후에는 복구가 사실상 불가능한 경우가 많다.
기존 백신 소프트웨어는 알려진 시그니처 기반으로 동작하기 때문에
**신종·변종 랜섬웨어**에 대한 대응이 느리다는 한계가 있다.

이 프로젝트는 다음 문제의식에서 출발했다.

> "파일이 암호화되는 **행위 자체**를 실시간으로 감지할 수 있다면,
> 시그니처 없이도 신종 랜섬웨어를 탐지할 수 있지 않을까?"

파일의 **엔트로피 변화**, **확장자 변경**, **대량 삭제·rename 패턴** 등
랜섬웨어가 암호화 과정에서 공통적으로 남기는 흔적을 머신러닝으로 학습해,
시그니처에 의존하지 않는 행위 기반 탐지 시스템을 구축하고자 했다.

---

## Algorithm | 알고리즘

### 시스템 순서도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              사용자 PC                                   │
│                                                                         │
│  ┌──────────┐   앱 실행    ┌──────────────────────────────────────────┐ │
│  │ 사용자   │ ──────────▶  │             Electron                     │ │
│  └──────────┘             │  Spring Boot JAR 자동 기동                │ │
│                           │  헬스체크 폴링 → 준비 완료 시 UI 표시     │ │
│                           └──────────────────┬───────────────────────┘ │
│                                              │                          │
│       감시 폴더 설정                          ▼                          │
│  ┌──────────┐  POST /settings  ┌────────────────────────┐              │
│  │  React   │ ───────────────▶ │    Spring Boot :8080    │              │
│  │  대시보드 │                  │                        │              │
│  └──────────┘                  │  WatcherService         │              │
│                                │  (Java NIO WatchService) │              │
│                                │  감시 폴더 재귀 등록     │              │
│                                └────────────┬───────────┘              │
│                                             │ 파일 이벤트               │
│  ┌──────────────────────────────────────────▼──────────────────────┐   │
│  │                     탐지 파이프라인                               │   │
│  │                                                                  │   │
│  │  [CREATE / MODIFY / DELETE 이벤트]                               │   │
│  │           │                                                      │   │
│  │           ▼                                                      │   │
│  │  FileCollectorService                                            │   │
│  │  ├─ 엔트로피 계산 (Shannon, 3구간 샘플링)                        │   │
│  │  ├─ 파일 크기 측정                                               │   │
│  │  ├─ 확장자 추출                                                  │   │
│  │  └─ 스냅샷 비교 (before → after)                                │   │
│  │           │                                                      │   │
│  │           ▼                                                      │   │
│  │  EventWindowAggregator  ◀── @Scheduled (3초 자동 flush)         │   │
│  │  └─ 3초 윈도우 집계 → 9개 피처 벡터 계산                        │   │
│  │           │                                                      │   │
│  └───────────┼──────────────────────────────────────────────────────┘   │
│              │ AiPayload (9 features)                                   │
└──────────────┼─────────────────────────────────────────────────────────┘
               │ HTTP POST /api/analyze
               ▼
┌──────────────────────────────────────┐
│   Python AI 서버 (Railway 클라우드)   │
│   FastAPI + XGBoost                  │
│                                      │
│   9개 피처 → 랜섬웨어 확률 score      │
│   score ≥ 0.70 → DANGER             │
│   score ≥ 0.50 → WARNING            │
│   score <  0.50 → SAFE              │
└──────────────┬───────────────────────┘
               │ label, score, topFamily
               ▼
┌──────────────────────────────────────────────────────────────────────┐
│                          결과 처리                                    │
│                                                                      │
│   SAFE     ──▶  로그 저장 (SQLite)                                   │
│                                                                      │
│   WARNING  ──▶  로그 저장 + 알림 저장 + 대시보드 표시                │
│                                                                      │
│   DANGER   ──▶  로그 저장 + 알림 저장                               │
│              ──▶  Gemini LLM → 맞춤 대응 가이드 생성                │
│              ──▶  이메일 경보 발송 (SMTP)                            │
│              ──▶  Electron 전체화면 경보창 팝업                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

### 시스템 설계도

```
┌────────────────────────────────────────────────────────────────────────┐
│                           사용자 PC                                     │
│                                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                        Electron 래퍼                             │  │
│  │                                                                 │  │
│  │  ┌─────────────┐   IPC   ┌─────────────────────────────────┐   │  │
│  │  │  main.js    │ ──────▶ │         BrowserWindow           │   │  │
│  │  │             │         │   http://localhost:8080 로드     │   │  │
│  │  │ - JAR 실행  │         └─────────────────────────────────┘   │  │
│  │  │ - 헬스 폴링 │                                                │  │
│  │  │ - DANGER폴링│   ┌──────────────────────────────────────┐   │  │
│  │  │ - 트레이    │   │       emergency.html (전체화면 경보)  │   │  │
│  │  └─────────────┘   └──────────────────────────────────────┘   │  │
│  └───────────────────────────────┬─────────────────────────────────┘  │
│                                  │ java -jar                           │
│                                  ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │                  Spring Boot :8080                               │  │
│  │                                                                 │  │
│  │  ┌──────────────┐  ┌───────────────┐  ┌─────────────────────┐  │  │
│  │  │   Watcher    │  │   Collector   │  │  EventWindow        │  │  │
│  │  │   Service    │─▶│   Service     │─▶│  Aggregator         │  │  │
│  │  │  NIO Watch   │  │  엔트로피/크기│  │  3초 윈도우 9피처   │  │  │
│  │  └──────────────┘  └───────────────┘  └──────────┬──────────┘  │  │
│  │                                                   │             │  │
│  │  ┌──────────────┐  ┌───────────────┐             │             │  │
│  │  │  Log Service │  │Notification   │◀────────────┘             │  │
│  │  │  (async 큐)  │  │Service        │  AiService                │  │
│  │  └──────┬───────┘  └───────┬───────┘  (HTTP 통신)             │  │
│  │         │                  │                                   │  │
│  │         └──────────┬───────┘                                   │  │
│  │                    ▼                                            │  │
│  │  ┌─────────────────────────────────────────────────────────┐   │  │
│  │  │                    SQLite  log.db                        │   │  │
│  │  │   logs │ notifications │ settings │ notices │ feedbacks  │   │  │
│  │  └─────────────────────────────────────────────────────────┘   │  │
│  │                                                                 │  │
│  │  ┌──────────────────────────────────────────────────────────┐  │  │
│  │  │                  React 프론트엔드 (정적 내장)              │  │  │
│  │  │  대시보드 │ 알림 │ 로그 │ 설정 │ 관리자페이지              │  │  │
│  │  └──────────────────────────────────────────────────────────┘  │  │
│  └─────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
                    │ HTTP POST (9개 피처)
                    ▼
     ┌──────────────────────────────────┐
     │  Python AI 서버  (Railway)        │
     │  FastAPI + XGBoost               │
     │  POST /api/analyze → score/label │
     │  POST /predict    → 패밀리 분류  │
     └──────────────────────────────────┘
                    │
                    ▼  (DANGER 시)
     ┌──────────────────────────────────┐
     │  Google Gemini 2.5 Flash         │
     │  대응 가이드 텍스트 생성          │
     └──────────────────────────────────┘
```

---

## Development | 요약

### 주요 기능

| 기능 | 설명 |
|------|------|
| 실시간 파일 감시 | Java NIO WatchService로 감시 폴더 재귀 감시 |
| 행위 기반 AI 탐지 | XGBoost 9개 피처 벡터로 랜섬웨어 판정 |
| 랜섬웨어 패밀리 분류 | WannaCry, LockBit, REvil 등 13종 분류 |
| 대응 가이드 생성 | DANGER 탐지 시 Gemini LLM이 맞춤 대응 방법 제시 |
| 긴급 전체화면 경보 | Electron이 DANGER 폴링 → 전체화면 경보창 팝업 |
| 이메일 알림 | DANGER 탐지 시 설정된 이메일로 경보 메일 자동 발송 |
| 수동 스캔 | 감시 폴더 전체를 즉시 검사, 진행률 실시간 표시 |
| 파일 권한 감사 | others 쓰기·실행 권한 파일 탐지 (macOS/Linux) |
| 감시 스케줄 | 특정 요일·시간대에만 감시 활성화 |
| 예외 규칙 | 경로·확장자 화이트리스트 등록으로 오탐 방지 |
| 관리자 페이지 | 전체 세션 로그·알림 조회, 공지사항·가이드 관리 |

### 배포 형태

- **macOS**: `.dmg` 인스톨러 (Intel / Apple Silicon)
- **Windows**: `.msi` 인스톨러
- AI 서버는 Railway 클라우드에 상시 배포, 별도 설치 불필요

---

## Tool | 개발환경

```
┌──────────────────────────────────────────────────────────────────────┐
│                         개발 환경 구성                                 │
│                                                                      │
│  ┌─────────────────────────┐   ┌──────────────────────────────────┐  │
│  │       Frontend          │   │           Desktop                │  │
│  │                         │   │                                  │  │
│  │  React 18 (CRA)         │   │  Electron                        │  │
│  │  JavaScript ES2022      │   │  electron-builder                │  │
│  │  CSS (glass-morphism)   │   │  .dmg / .msi 인스톨러            │  │
│  │  IDE: VS Code           │   │                                  │  │
│  └────────────┬────────────┘   └──────────────┬───────────────────┘  │
│               │ 정적 파일 내장                  │ JAR 번들             │
│               └──────────────┬─────────────────┘                     │
│                              ▼                                        │
│  ┌───────────────────────────────────────────────────────────────┐    │
│  │                        Backend                                │    │
│  │                                                               │    │
│  │  Java 17          Spring Boot 3.5       Gradle               │    │
│  │  SQLite           Spring JDBC           IntelliJ IDEA         │    │
│  └───────────────────────────────────────────────────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
                              │ HTTP REST
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      AI / 외부 서비스                                  │
│                                                                      │
│  ┌──────────────────────────┐   ┌────────────────────────────────┐   │
│  │      AI 서버 (Railway)   │   │     Google Gemini API          │   │
│  │                          │   │                                │   │
│  │  Python 3.9              │   │  Gemini 2.5 Flash              │   │
│  │  FastAPI + Uvicorn       │   │  DANGER 탐지 시 대응 가이드     │   │
│  │  XGBoost (탐지 / 분류)   │   │  생성                          │   │
│  │  Railway Cloud PaaS      │   │                                │   │
│  └──────────────────────────┘   └────────────────────────────────┘   │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

| 계층 | 언어 / 프레임워크 | 도구 |
|------|-----------------|------|
| Frontend | JavaScript, React 18 | VS Code, CRA |
| Backend | Java 17, Spring Boot 3.5 | IntelliJ IDEA, Gradle |
| Desktop | Electron | electron-builder |
| AI 서버 | Python 3.9, FastAPI, XGBoost | Railway |
| LLM | Google Gemini 2.5 Flash | Gemini API |
| DB | SQLite | Spring JDBC |
