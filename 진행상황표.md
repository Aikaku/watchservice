# 2026년 1학기 주별 진행 상황

> 기준일: 2026-03-21 | 전체 작업: 38개 (완료 20개 / 미완 18개) | 전체 진행률: 53%

---

| 내용 | 설명 | 왜 필요한지 | 1~3주 | 3~5주 | 7~9주 | 9~11주 | 11~13주 | 13~15주 | 비고 |
|:-----|:-----|:-----------|:-----:|:-----:|:-----:|:------:|:-------:|:-------:|:-----|
| **[보안]** | | | | | | | | | |
| BCrypt 해싱 | 관리자 비밀번호를 BCrypt 해시로 저장·검증 | 평문 비밀번호가 유출되면 즉시 악용 가능. 해시 저장 시 원문 복원 불가 | 100% | | | | | | `AdminAuthService.java` — `$2a$`/`$2b$` 시작 시 BCrypt, 아니면 평문 분기 |
| Spring Security 도입 | Spring Security 적용 및 보안 헤더 활성화 | 클릭재킹·MIME 스니핑 등 기본 웹 공격을 프레임워크 수준에서 자동 차단 | 100% | | | | | | `SecurityConfig.java` — X-Frame-Options DENY, X-Content-Type-Options nosniff 등 |
| 세션 타임아웃 설정 | 비활동 관리자 세션 자동 만료 | 방치된 브라우저로 관리자 페이지 무단 접근 방지 | 100% | | | | | | `server.servlet.session.timeout: 30m` |
| .env git 추적 확인 | 민감 정보 파일의 git 추적 여부 검증 | .env가 커밋되면 API 키·비밀번호가 git 이력에 영구 노출됨 | 100% | | | | | | `.gitignore`에 `.env`, `*.env` 포함 확인 완료 |
| 평문 비교 폴백 제거 | BCrypt 검증만 허용, 평문 비교 코드 삭제 | 폴백이 남아 있으면 평문 비밀번호 설정 시 BCrypt 우회 가능 | | | | | | | `AdminAuthService.java:45` — `adminPassword.equals(password)` 미제거 |
| 기본 비밀번호 폴백 제거 | 환경변수 미설정 시 서버 기동 실패로 강제 | 기본값 `123456789`로 운영 서버가 뜨면 즉시 관리자 인증 우회 가능 | | | | | | | `application.yml` — `${ADMIN_PASSWORD:123456789}` 폴백 미제거 |
| **[백엔드]** | | | | | | | | | |
| JFileChooser 제거 | Swing GUI 다이얼로그 제거 후 API 기반 파일 탐색으로 교체 | Swing은 GUI 환경 필수 — 서버·Docker·SSH 환경에서 기동 자체 불가 | 100% | | | | | | 서버 파일시스템 탐색 모달로 교체 |
| headless 모드 설정 | `spring.main.headless: true` 설정 | GUI 없는 서버 환경에서 AWT 초기화 오류 없이 Spring 기동 가능 | 100% | | | | | | `application.yml` |
| 환경변수 전달 수정 | Gradle `bootRun`에 시스템 환경변수 전달 | 수정 전에는 `.env` 값이 Spring에 주입되지 않아 API 키·비밀번호 미적용 | 100% | | | | | | `build.gradle` — `bootRun { environment System.getenv() }` |
| AI 서버 타임아웃 설정 | RestTemplate에 connect/read 타임아웃 설정 | 타임아웃 없으면 AI 서버 무응답 시 요청 스레드가 무한 대기 — 서버 전체 행 | 100% | | | | | | connect 5s / read 15s, `AI_CONNECT_TIMEOUT_MS` 환경변수 조정 가능 |
| CORS 환경변수화 | 전 컨트롤러 `@CrossOrigin` 제거, `WebConfig` 중앙 관리 | 하드코딩된 `localhost`가 운영 도메인에서 CORS 차단을 유발 | 100% | | | | | | `CORS_ALLOWED_ORIGINS` 환경변수로 도메인 지정 |
| SQLite WAL 모드 | SQLite WAL(Write-Ahead Logging) 모드 활성화 | 기본 DELETE 모드는 쓰기 중 읽기 차단 — 랜섬웨어 탐지 중 로그 조회 시 락 충돌 | 100% | | | | | | `SqliteConfig @PostConstruct` — `PRAGMA journal_mode=WAL` |
| 로그 파일 롤링 | 로그 파일 크기·날짜 기반 자동 롤링 설정 | 롤링 없으면 장기 운영 시 로그 파일이 디스크를 가득 채워 서버 중단 가능 | 100% | | | | | | `logback-spring.xml` — 50MB / 30일 / 최대 2GB |
| Actuator 노출 제한 | `/health`만 외부 노출, 나머지 엔드포인트 차단 | 기본 설정은 메모리·스레드·환경변수 등 내부 정보 전체 노출 — 공격자에 정보 제공 | 100% | | | | | | `management.endpoints.web.exposure.include: health` |
| 알림 통계 API 구현 | `GET /notifications/stats` — ai_label별 집계 API | 프론트 통계 페이지가 하드코딩 데이터만 표시하던 상태 — 실데이터 연동 필요 | 100% | | | | | | ai_label별 GROUP BY 집계, 프론트 차트 연동 완료 |
| GlobalExceptionHandler | `@ControllerAdvice`로 전역 예외 처리 통일 | 컨트롤러마다 try-catch 방식이 달라 에러 응답 형태가 불규칙 — 클라이언트 파싱 불가 | | | | | | | `@ControllerAdvice` 미구현 |
| API 응답형식 통일 | `ApiResponse<T>` 래퍼 클래스로 응답 형식 표준화 | `List`, `String`, `Map`, 커스텀 DTO 혼용 — 프론트에서 응답 구조 예측 불가 | | | | | | | `List`, `String`, `Map.of("result","ok")` 등 혼용 중 |
| 입력 검증 (Bean Validation) | DTO에 `@Valid`, `@NotNull` 등 어노테이션 적용 | 검증 없으면 악의적인 파라미터로 파일시스템 경로 조작·DB 오류 유발 가능 | | | | | | | `/settings/browse?path=` 경로 파라미터도 무검증 |
| **[프론트엔드]** | | | | | | | | | |
| alert() → Toast 교체 | 브라우저 `alert()` 대신 인라인 Toast 알림으로 교체 | `alert()`은 UI를 블로킹하고 스타일 커스터마이징 불가 — UX 저하 | 100% | | | | | | `Toast.jsx` + `ToastProvider` 구현, 전 페이지 적용 완료 |
| 알림 통계 페이지 | 알림 통계 차트 페이지 실데이터 연동 | 하드코딩 더미 데이터만 표시되던 상태 — 실제 탐지 현황 확인 불가 | 100% | | | | | | `NotificationStatsPage` — API 호출 연동 완료 |
| ProtectedRoute 구현 | `/admin/**` 라우트에 세션 인증 가드 추가 | 현재 URL 직접 입력으로 관리자 페이지 진입 가능 — 백엔드 세션 확인 없이 렌더링 | | | | | | | `App.js` — `Route` 그대로 노출 중 |
| useNavigate() 교체 | 로그아웃 시 `window.location.href` → `useNavigate()`로 교체 | 전체 새로고침으로 React 상태 초기화 — SPA 특성 파괴, 깜빡임 발생 | | | | | | | `AdminMainPage.jsx:16` |
| fetch 타임아웃 설정 | `HttpClient.js`에 `AbortSignal.timeout()` 추가 | AI 서버 무응답 시 fetch가 무한 대기 — UI 전체가 응답 없는 상태로 멈춤 | | | | | | | `HttpClient.js` — 타임아웃 미설정 |
| HTML 메타데이터 수정 | `index.html` title·description을 서비스명으로 변경 | CRA 기본값(`React App`) 그대로면 브라우저 탭·검색엔진에 서비스명 미노출 | | | | | | | `public/index.html` — title=`React App` 그대로 |
| 버전 확인 기능 | 버전 체크 버튼 실제 로직 구현 또는 제거 | "나중에 구현" 토스트만 표시 — 버튼이 있어 사용자가 클릭하는데 아무 동작 없음 | | | | | | | `SettingUpdatePage.jsx` — 미구현 상태 |
| ErrorBoundary 추가 | `App.js` 최상위에 `ErrorBoundary` 컴포넌트 추가 | 바운더리 없으면 컴포넌트 런타임 에러 한 건에 전체 앱 흰 화면 크래시 | | | | | | | `App.js` — ErrorBoundary 미적용 |
| .env.production 생성 | 프로덕션 빌드용 환경변수 파일 또는 CI/CD 주입 설정 | 미설정 시 `REACT_APP_API_BASE_URL`이 `localhost:8080`으로 고정 — 배포 즉시 API 전체 실패 | | | | | | | `HttpClient.js` 기본값 `localhost:8080` |
| **[AI 서버]** | | | | | | | | | |
| Gemini 타임아웃 설정 | Gemini API 호출에 타임아웃 설정 | 타임아웃 없으면 Gemini 응답 지연 시 백엔드 스레드 무한 대기 | 100% | | | | | | `timeoutMs=10000`, `thinkingBudget=0` |
| AI 서버 CORS 제한 | `allow_origins=["*"]` → 환경변수 기반 출처 제한 | 모든 출처 허용 시 외부에서 AI 서버에 직접 예측 요청 가능 — 모델 무단 사용 | 100% | | | | | | `os.getenv("CORS_ALLOWED_ORIGINS", "http://localhost:8080")` |
| score 계산 오류 수정 | 랜섬웨어 총합 확률로 score 계산하도록 변경 | top-1이 benign이어도 랜섬웨어 총합 확률이 높으면 SAFE 오판 — 실제 위협 미탐지 | 100% | | | | | | `sum(prob for item if family != "benign")` |
| benign 판정 로직 수정 | label을 score 임계값 기반으로 결정 | 문자열 `"benign" in top_family`로 판정 시 클래스명 변경에 취약 | 100% | | | | | | ≥0.70 DANGER / ≥0.50 WARNING / else SAFE |
| requirements.txt 작성 | Python 의존성 파일 생성 | 파일 없으면 새 환경에서 어떤 라이브러리가 필요한지 알 수 없어 즉시 실행 불가 | | | | | | | `fastapi`, `uvicorn`, `xgboost`, `lightgbm`, `pandas`, `numpy`, `pyyaml` |
| config.yaml 작성 | `configs/config.yaml` 파일 생성 | `api_server.py:60`에서 참조, 파일 없으면 서버 기동 시 예외 발생 가능 | | | | | | | `CONFIG_PATH = os.getenv("CONFIG_PATH", "configs/config.yaml")` |
| 누락 피처 처리 개선 | 누락 피처 채움 방식을 학습 시 결측 처리 방식과 통일 | `-1`은 이상한 값 — 모델이 0·NaN·mean으로 학습됐다면 예측 정확도 저하 | | | | | | | `features.get(c, -1)` — 학습 결측 처리 방식과 불일치 가능 |
| 모델 로드 실패 처리 | 모델 없이 기동 시 명시적 실패 또는 `/health` 503 반환 | 현재는 모델 없어도 서버가 정상 기동 — 백엔드가 `UNKNOWN`을 오류로 처리 안 하면 탐지 전체 무력화 | | | | | | | 모델 없이 기동 시 모든 요청에 `UNKNOWN` 반환 중 |
| **[배포·환경설정]** | | | | | | | | | |
| HTTPS 가이드 | SSL 설정 가이드 주석 작성 | HTTP 운영 시 관리자 세션 쿠키·탐지 데이터가 평문으로 전송 — 스니핑 가능 | 100% | | | | | | `application.yml`에 Spring Boot SSL / Nginx 리버스 프록시 주석 추가 |
| CORS dev 주소 제거 | 배포 시 `CORS_ALLOWED_ORIGINS` 환경변수를 실제 도메인으로 설정 | 기본값에 `localhost:5173`(Vite dev)이 포함 — 다른 클라이언트에서 API 호출 가능 | | | | | | | `allowed-origins` 기본값에 `localhost:5173` 포함 중 |
| Docker/docker-compose 구성 | `Dockerfile` × 3 + `docker-compose.yml` 작성 | 3개 서버를 수동으로 각각 기동해야 함 — 신규 환경 셋업·재배포 시 절차 복잡 | | | | | | | Spring Boot·FastAPI·React 모두 별도 수동 실행 |
| **총 진행상황(%)** | | | **53%** | | | | | | |
