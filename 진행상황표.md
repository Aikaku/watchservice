# 2026년 1학기 주별 진행 상황

> 기준일: 2026-03-21 | 전체 작업: 38개 (완료 20개 / 미완 18개) | 전체 진행률: 53%

---

| 내용 | 1~3주 | 3~5주 | 7~9주 | 9~11주 | 11~13주 | 13~15주 | 비고 |
|:-----|:-----:|:-----:|:-----:|:------:|:-------:|:-------:|:-----|
| **[보안]** | | | | | | | |
| BCrypt 해싱 | 100% | | | | | | `ADMIN_PASSWORD` 값이 `$2a$`/`$2b$`로 시작하면 BCrypt 검증, 아니면 평문 비교 분기 유지 |
| Spring Security 도입 | 100% | | | | | | CSRF disable, X-Frame-Options DENY, X-Content-Type-Options nosniff 등 보안 헤더 활성화 |
| 세션 타임아웃 설정 | 100% | | | | | | `server.servlet.session.timeout: 30m` |
| .env git 추적 확인 | 100% | | | | | | `.gitignore`에 `.env`, `*.env` 포함 확인 완료 |
| 평문 비교 폴백 제거 | | | | | | | `AdminAuthService.java:45`의 `adminPassword.equals(password)` 미제거 — 평문 환경변수 설정 시 평문 비교로 동작 |
| 기본 비밀번호 폴백 제거 | | | | | | | `application.yml`의 `${ADMIN_PASSWORD:123456789}` — 환경변수 미설정 시 약한 기본값으로 서버 기동 |
| **[백엔드]** | | | | | | | |
| JFileChooser 제거 | 100% | | | | | | Swing 다이얼로그 제거 후 서버 파일시스템 탐색 모달로 교체 — headless 서버·Docker 환경 지원 |
| headless 모드 설정 | 100% | | | | | | `spring.main.headless: true` — Swing GUI 없는 환경에서 기동 가능 |
| 환경변수 전달 수정 | 100% | | | | | | `bootRun { environment System.getenv() }` — `.env` 값이 Spring에 정상 주입됨 |
| AI 서버 타임아웃 설정 | 100% | | | | | | `SimpleClientHttpRequestFactory` connect 5s / read 15s, 환경변수(`AI_CONNECT_TIMEOUT_MS` 등)로 조정 가능 |
| CORS 환경변수화 | 100% | | | | | | 전 컨트롤러 `@CrossOrigin` 제거, `WebConfig`에서 `CORS_ALLOWED_ORIGINS`로 중앙 관리 |
| SQLite WAL 모드 | 100% | | | | | | `SqliteConfig @PostConstruct`에서 `PRAGMA journal_mode=WAL` 실행 — 동시 읽기 성능 향상 |
| 로그 파일 롤링 | 100% | | | | | | `logback-spring.xml` SizeAndTimeBasedRollingPolicy (50MB / 30일 / 최대 2GB) |
| Actuator 노출 제한 | 100% | | | | | | `management.endpoints.web.exposure.include: health`만 외부 노출, 상세 정보 숨김 |
| 알림 통계 API 구현 | 100% | | | | | | `GET /notifications/stats` — ai_label별 GROUP BY 집계, 프론트 차트 연동 완료 |
| GlobalExceptionHandler | | | | | | | `@ControllerAdvice` 미구현 — 일부 컨트롤러는 try-catch, 일부는 예외 그냥 throw로 응답 형태 불일치 |
| API 응답형식 통일 | | | | | | | `List`, `String`, `Map.of("result","ok")`, 커스텀 DTO 혼용 — `ApiResponse<T>` 래퍼 클래스 도입 필요 |
| 입력 검증 (Bean Validation) | | | | | | | `@Valid`, `@NotNull` 등 전무 — `/settings/browse?path=` 파일시스템 경로 파라미터도 무검증 |
| **[프론트엔드]** | | | | | | | |
| alert() → Toast 교체 | 100% | | | | | | 자체 `Toast.jsx` + `ToastProvider` 구현, 전 페이지 `alert()` 완전 제거 |
| 알림 통계 페이지 | 100% | | | | | | `NotificationsApi.js` 실제 API 호출 연동, `NotificationStatsPage` 차트 구현 완료 |
| ProtectedRoute 구현 | | | | | | | `/admin/**` 라우트가 프론트에서 인증 없이 접근 가능 — `App.js`에 백엔드 세션 확인 후 렌더링하는 ProtectedRoute 추가 필요 |
| useNavigate() 교체 | | | | | | | `AdminMainPage.jsx:16` 로그아웃 시 `window.location.href`로 전체 페이지 새로고침 — SPA 상태 유실 |
| fetch 타임아웃 설정 | | | | | | | `HttpClient.js`의 `fetch()`에 타임아웃 없음 — AI 서버 무응답 시 무한 대기, `AbortSignal.timeout(10000)` 추가 필요 |
| HTML 메타데이터 수정 | | | | | | | `public/index.html` title=`React App`, description=`Web site created using create-react-app` — CRA 기본값 그대로 |
| 버전 확인 기능 | | | | | | | `SettingUpdatePage.jsx` 버전 체크 버튼 클릭 시 "나중에 구현" 토스트만 표시 — 실제 로직 구현 또는 버튼 제거 필요 |
| ErrorBoundary 추가 | | | | | | | 에러 바운더리 미구현 — 컴포넌트 런타임 에러 발생 시 전체 앱 크래시, `App.js` 최상위에 `ErrorBoundary` 필요 |
| .env.production 생성 | | | | | | | `REACT_APP_API_BASE_URL` 기본값이 `localhost:8080` — 프로덕션 빌드 시 실서버 주소 주입 방법 없음 |
| **[AI 서버]** | | | | | | | |
| Gemini 타임아웃 설정 | 100% | | | | | | `timeoutMs=10000`, `thinkingBudget=0` 설정 완료 |
| AI 서버 CORS 제한 | 100% | | | | | | `allow_origins=["*"]` → `os.getenv("CORS_ALLOWED_ORIGINS", "http://localhost:8080")`로 수정 완료 |
| score 계산 오류 수정 | 100% | | | | | | `ransomware_score = sum(prob for item if item.family != "benign")`로 수정 — 다중 클래스 오판 해결 |
| benign 판정 로직 수정 | 100% | | | | | | 레이블을 score 임계값 기반(≥0.70 DANGER / ≥0.50 WARNING / else SAFE)으로 변경 완료 |
| requirements.txt 작성 | | | | | | | `코드/` 폴더에 Python 의존성 파일 없음 — `fastapi`, `uvicorn`, `xgboost`, `lightgbm`, `pandas`, `numpy`, `pyyaml` 필요 |
| config.yaml 작성 | | | | | | | `api_server.py:60`에서 `configs/config.yaml` 참조, 파일 미존재 시 기동 시 예외 발생 가능 |
| 누락 피처 처리 개선 | | | | | | | `features.get(c, -1)` — -1은 유효하지 않은 값, 모델 학습 시 결측 처리 방식(0·NaN·mean)과 반드시 일치 필요 |
| 모델 로드 실패 처리 | | | | | | | 모델 없이 서버 기동 시 모든 요청에 `UNKNOWN` 반환 — 명시적 실패 처리 또는 `/health` 503 반환 필요 |
| **[배포·환경설정]** | | | | | | | |
| HTTPS 가이드 | 100% | | | | | | `application.yml`에 Spring Boot SSL / Nginx 리버스 프록시 설정 방법 주석 추가 완료 |
| CORS dev 주소 제거 | | | | | | | `allowed-origins` 기본값에 `localhost:5173`(Vite dev) 포함 — 배포 시 `CORS_ALLOWED_ORIGINS` 환경변수 실제 도메인으로 설정 필수 |
| Docker/docker-compose 구성 | | | | | | | Spring Boot·FastAPI·React 3개 서버 수동 실행 필요 — `Dockerfile` × 3 및 `docker-compose.yml` 미존재 |
| **총 진행상황(%)** | **53%** | | | | | | |
