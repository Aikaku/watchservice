# 2026년 1학기 주별 진행 상황

> 기준일: 2026-03-21 | 전체 작업: 40개 (완료 20개 / 미완 20개) | 전체 진행률: 50%

---

| 내용 | 설명 | 안 하면 어떻게 되는지 | 1~3주 | 3~5주 | 7~9주 | 9~11주 | 11~13주 | 13~15주 | 비고 |
|:-----|:-----|:-----------|:-----:|:-----:|:-----:|:------:|:-------:|:-------:|:-----|
| **[보안]** | | | | | | | | | |
| BCrypt 해싱 | 관리자가 로그인할 때 `AdminAuthService`가 입력 비밀번호를 받아 `$2a$`/`$2b$` 시작 여부로 BCrypt 해시인지 판단하고, 해시면 `BCryptPasswordEncoder.matches()`로, 아니면 평문 비교로 검증한다 | `.env` 파일이나 DB가 유출되면 비밀번호가 그대로 읽혀서 관리자 계정을 즉시 탈취당함. BCrypt는 단방향이라 유출돼도 원문을 알 수 없음 | 100% | | | | | | `AdminAuthService.java` |
| Spring Security 도입 | `SecurityConfig`에서 Spring Security를 적용해 모든 HTTP 응답에 X-Frame-Options DENY, X-Content-Type-Options nosniff 등 보안 헤더를 자동 삽입하고 CSRF는 REST API 특성상 disable 처리한다 | 공격자가 이 사이트를 다른 사이트 안에 몰래 iframe으로 심어서 관리자 클릭을 가로채거나(클릭재킹), 브라우저가 응답 파일을 스크립트로 잘못 실행할 수 있음 | 100% | | | | | | `SecurityConfig.java` |
| 세션 타임아웃 설정 | `application.yml`의 `server.servlet.session.timeout: 30m`으로 설정하여 관리자가 30분간 아무 동작을 하지 않으면 서버가 자동으로 세션을 만료시킨다 | 관리자가 로그인한 채 자리를 비우면 그 PC에 접근한 누구든 관리자 기능을 무제한으로 사용할 수 있음 | 100% | | | | | | `application.yml` |
| .env git 추적 확인 | `.gitignore`에 `.env`, `*.env`가 등록되어 있는지 확인하여 `ADMIN_PASSWORD`, `GEMINI_API_KEY` 등 민감 값이 git 커밋 대상에서 제외되는지 검증했다 | 실수로 `git push`하면 비밀번호·API 키가 GitHub에 올라가 누구나 볼 수 있고, 나중에 파일을 삭제해도 커밋 이력에서 영구 복원 가능 | 100% | | | | | | `.gitignore` 확인 완료 |
| 평문 비교 폴백 제거 | `AdminAuthService.java:45`의 `return adminPassword.equals(password)` 분기를 제거하여 BCrypt 해시가 아닌 환경변수로 설정하더라도 반드시 BCrypt만 허용하도록 강제해야 한다 | 운영자가 환경변수에 평문 비밀번호를 그대로 넣으면 BCrypt 검증을 건너뛰고 평문 비교로 동작해 BCrypt 도입이 의미 없어짐 | | | | | | | `AdminAuthService.java:45` 미제거 |
| 기본 비밀번호 폴백 제거 | `application.yml`의 `${ADMIN_PASSWORD:123456789}` 기본값을 제거하거나, 환경변수가 없을 경우 서버 기동을 실패시키는 유효성 검사를 `@PostConstruct`에 추가해야 한다 | 환경변수 설정을 빠뜨리고 서버를 켜면 비밀번호가 `123456789`로 고정된 채 운영되어 관리자 페이지에 누구든 접근 가능 | | | | | | | `${ADMIN_PASSWORD:123456789}` 미제거 |
| **[백엔드]** | | | | | | | | | |
| JFileChooser 제거 | 기존에 감시 폴더를 선택할 때 서버 프로세스가 `JFileChooser` Swing 다이얼로그를 직접 띄우던 방식을, 백엔드가 파일시스템 경로 목록을 API로 반환하고 프론트가 모달로 탐색하는 방식으로 교체했다 | 모니터가 없는 서버(AWS EC2, Docker 컨테이너, SSH 원격 접속 환경)에서는 서버 자체가 시작되지 않음 | 100% | | | | | | 서버 파일시스템 탐색 모달로 교체 |
| headless 모드 설정 | `application.yml`에 `spring.main.headless: true`를 추가하여 Spring Boot가 JVM AWT를 headless 모드로 초기화하도록 설정했다 | 화면 없는 서버에서 Spring Boot 시작 시 `HeadlessException` 오류가 발생해 서버가 켜지지 않음 | 100% | | | | | | `application.yml` |
| 환경변수 전달 수정 | `build.gradle`의 `bootRun` 태스크에 `environment System.getenv()`를 추가하여 OS 환경변수(`.env` 포함)가 Spring Boot 프로세스에 그대로 전달되도록 수정했다 | `.env`에 설정한 `GEMINI_API_KEY`, `ADMIN_PASSWORD` 등이 Spring에 전달되지 않아 Gemini AI 응답, 관리자 인증 등 핵심 기능이 전혀 동작하지 않음 | 100% | | | | | | `build.gradle` |
| AI 서버 타임아웃 설정 | `AiService`의 `RestTemplate`에 `SimpleClientHttpRequestFactory`를 적용하여 연결 타임아웃 5초, 읽기 타임아웃 15초를 설정하고, `AI_CONNECT_TIMEOUT_MS`·`AI_READ_TIMEOUT_MS` 환경변수로 조정 가능하게 했다 | AI 서버가 꺼져 있거나 응답이 느릴 때 Spring 스레드가 무한 대기하다가 스레드 풀이 소진되어 결국 서버 전체가 아무 요청도 처리하지 못하는 상태로 멈춤 | 100% | | | | | | connect 5s / read 15s |
| CORS 환경변수화 | 8개 컨트롤러에 흩어져 있던 `@CrossOrigin` 어노테이션을 모두 제거하고 `WebConfig`에서 `CORS_ALLOWED_ORIGINS` 환경변수를 읽어 허용 출처를 한 곳에서 관리하도록 중앙화했다 | 운영 도메인으로 배포하면 `localhost:3000`이 하드코딩되어 있어 모든 API 요청이 브라우저 CORS 정책에 막혀 화면에 데이터가 전혀 뜨지 않음 | 100% | | | | | | `WebConfig.java` |
| SQLite WAL 모드 | `SqliteConfig`의 `@PostConstruct`에서 `PRAGMA journal_mode=WAL`을 실행하여 SQLite를 WAL(Write-Ahead Logging) 모드로 전환했다 | 랜섬웨어 탐지 이벤트가 DB에 기록되는 순간 사용자가 로그 페이지를 열면 DB 잠금 충돌로 쿼리 오류가 발생 | 100% | | | | | | `SqliteConfig.java` |
| 로그 파일 롤링 | `logback-spring.xml`에 `SizeAndTimeBasedRollingPolicy`를 설정하여 로그 파일이 50MB를 초과하거나 날짜가 바뀌면 자동으로 새 파일로 교체되고, 30일 초과·총 2GB 초과 파일은 자동 삭제된다 | 수개월 운영 후 로그 파일이 수십 GB가 되어 디스크를 가득 채우면 OS 로그 기록도, DB 저장도 실패해서 서버 전체 기능이 마비됨 | 100% | | | | | | `logback-spring.xml` |
| Actuator 노출 제한 | `application.yml`에서 `management.endpoints.web.exposure.include: health`만 설정하고 `show-details: never`로 지정하여 `/actuator/health`만 외부에서 접근 가능하고 내부 상세 정보는 숨겼다 | `/actuator` 주소에 접근하면 서버 메모리 사용량, 환경변수 목록, 내부 빈 구조 등이 그대로 노출되어 공격자가 서버 구조를 쉽게 파악 가능 | 100% | | | | | | `application.yml` |
| 알림 통계 API 구현 | 백엔드에 `GET /notifications/stats`를 구현하여 ai_label(`SAFE`/`WARNING`/`DANGER`)별로 알림 건수를 GROUP BY 집계해 반환하고, `NotificationStatsPage`가 해당 API를 호출해 차트로 시각화한다 | 통계 페이지에 항상 가짜 숫자만 표시되어 실제 랜섬웨어 탐지 현황을 전혀 파악할 수 없음 | 100% | | | | | | `NotificationController.java` |
| GlobalExceptionHandler | `@ControllerAdvice`를 구현하여 모든 컨트롤러에서 발생하는 예외를 한 곳에서 캐치하고, 일관된 에러 응답 형식(`{ code, message }`)으로 반환하도록 통일해야 한다 | API 오류 발생 시 어떤 엔드포인트는 JSON을, 어떤 건 HTML 에러 페이지를 반환해서 프론트가 에러 내용을 제대로 파싱하지 못하고 오류 원인을 알 수 없게 됨 | | | | | | | `@ControllerAdvice` 미구현 |
| API 응답형식 통일 | 모든 컨트롤러가 `ApiResponse<T>` 래퍼 클래스를 통해 `{ success, data, message }` 형태로 응답을 반환하도록 통일하고, 기존에 `List`, `String`, `Map` 등으로 제각각 반환하던 코드를 교체해야 한다 | API마다 응답 구조가 달라 프론트에서 각각 다르게 파싱해야 하고, 새 API를 추가할 때마다 응답 형식을 매번 따로 정해야 해서 유지보수가 점점 어려워짐 | | | | | | | `List`, `String`, `Map.of()` 혼용 중 |
| 입력 검증 (Bean Validation) | DTO 클래스에 `@NotNull`, `@NotBlank`, `@Size` 등 Bean Validation 어노테이션을 추가하고, 컨트롤러에 `@Valid`를 적용하여 잘못된 요청이 서비스 계층에 도달하기 전에 차단해야 한다 | 공격자가 경로 파라미터에 `../../../../etc/passwd` 같은 값을 넣어 서버 내부 파일을 탐색하거나, 예상치 못한 값으로 DB 오류를 유발할 수 있음 | | | | | | | `/settings/browse?path=` 경로 파라미터 무검증 |
| **[프론트엔드]** | | | | | | | | | |
| alert() → Toast 교체 | 전 페이지에서 `alert()` 호출을 찾아 제거하고, 자체 구현한 `Toast.jsx` + `ToastProvider`로 교체하여 화면 우측 상단에 비차단형 알림이 표시되도록 했다 | 오류나 알림이 뜰 때마다 브라우저 팝업이 화면 전체를 멈추고, 사용자가 확인 버튼을 누르기 전까지 다른 어떤 동작도 불가능 | 100% | | | | | | 전 페이지 `alert()` 제거 완료 |
| 알림 통계 페이지 | `NotificationsApi.js`의 하드코딩 데이터를 제거하고 `GET /notifications/stats` API를 실제 호출하도록 교체하여, `NotificationStatsPage`의 차트가 실시간 탐지 데이터를 반영하도록 구현했다 | 통계 차트에 항상 같은 가짜 숫자가 표시되어 실제 랜섬웨어 탐지 현황을 파악하는 것이 불가능 | 100% | | | | | | `NotificationStatsPage.jsx` 연동 완료 |
| ProtectedRoute 구현 | `App.js`에서 `/admin/**` 라우트를 `ProtectedRoute` 컴포넌트로 감싸고, 렌더링 전에 백엔드 세션 유효성 확인 API를 호출하여 미인증 상태이면 `/admin/login`으로 리다이렉트하도록 구현해야 한다 | 로그인하지 않아도 `/admin/main`을 주소창에 직접 입력하면 관리자 페이지 화면이 그대로 렌더링됨. API는 세션으로 막히지만 UI 레이아웃·버튼·메뉴 구조가 모두 노출 | | | | | | | `App.js` — 일반 `Route`로 노출 중 |
| useNavigate() 교체 | `AdminMainPage.jsx:16`의 로그아웃 핸들러에서 `window.location.href = '/admin/login'`을 `const navigate = useNavigate(); navigate('/admin/login')`으로 교체하여 React Router 내부 라우팅을 사용하도록 수정해야 한다 | 로그아웃할 때마다 전체 페이지가 새로고침되어 화면이 깜빡이고 React가 보관하던 모든 상태가 초기화됨 | | | | | | | `AdminMainPage.jsx:16` |
| fetch 타임아웃 설정 | `HttpClient.js`의 모든 `fetch()` 호출에 `signal: AbortSignal.timeout(10000)`을 추가하여 10초 내 응답이 없으면 요청을 자동으로 중단하고 에러를 발생시키도록 설정해야 한다 | AI 서버가 꺼져 있거나 응답이 느릴 때 화면의 로딩 스피너가 영원히 돌고, 사용자는 강제 새로고침 외에 멈춘 화면에서 벗어날 방법이 없음 | | | | | | | `HttpClient.js` — 타임아웃 미설정 |
| HTML 메타데이터 수정 | `public/index.html`의 `<title>React App</title>`을 서비스명으로, `<meta name="description">`을 서비스 설명으로 변경하여 브라우저 탭과 검색엔진에 올바른 정보가 표시되도록 수정해야 한다 | 브라우저 탭에 "React App"이라고 표시되어 사용자가 탭을 구분하기 어렵고, 외부 공개 시 검색엔진에 "Web site created using create-react-app"이 설명으로 노출됨 | | | | | | | `public/index.html` — CRA 기본값 그대로 |
| 버전 확인 기능 | `SettingUpdatePage.jsx`의 버전 체크 버튼에 실제 버전 확인 로직을 구현하거나, 구현 계획이 없다면 버튼 자체를 제거해야 한다 | 설정 페이지에서 버전 체크 버튼을 눌러도 "나중에 구현" 토스트만 뜨는 미완성 기능이 그대로 배포되어 사용자에게 혼란을 줌 | | | | | | | `SettingUpdatePage.jsx` — 미구현 토스트만 표시 |
| ErrorBoundary 추가 | React의 `ErrorBoundary` 컴포넌트를 구현하여 `App.js`에서 전체 라우트를 감싸고, 하위 컴포넌트에서 런타임 에러 발생 시 흰 화면 대신 에러 안내 페이지를 표시하도록 해야 한다 | 특정 화면에서 코드 오류가 하나라도 발생하면 앱 전체가 흰 화면으로 크래시되고 새로고침 외에 복구 방법이 없음 | | | | | | | `App.js` — ErrorBoundary 미적용 |
| .env.production 생성 | `watchservice_fe/` 루트에 `.env.production` 파일을 만들어 `REACT_APP_API_BASE_URL=https://실서버주소`를 지정하거나, CI/CD 파이프라인에서 빌드 시 환경변수를 주입하는 방법을 설정해야 한다 | `npm run build`로 빌드한 프론트가 `localhost:8080`으로 API를 요청해서, 배포 즉시 모든 데이터 로딩이 실패하고 화면이 빈 상태로 나옴 | | | | | | | `HttpClient.js` 기본값 `localhost:8080` |
| **[AI 서버]** | | | | | | | | | |
| Gemini 타임아웃 설정 | `GeminiAdviceService`에서 Gemini API를 호출할 때 타임아웃을 10초로 설정하고 `thinkingBudget=0`으로 지정하여 응답 시간을 제한했다 | Gemini API가 느릴 때 백엔드 스레드가 응답을 무한 대기하다가 다른 탐지 요청들도 함께 처리하지 못하게 되어 서비스 전체가 느려짐 | 100% | | | | | | `timeoutMs=10000`, `thinkingBudget=0` |
| AI 서버 CORS 제한 | `api_server.py`에서 `allow_origins=["*"]`를 제거하고 `os.getenv("CORS_ALLOWED_ORIGINS", "http://localhost:8080")`으로 교체하여 Spring Boot 서버 주소에서의 요청만 허용하도록 수정했다 | 외부 누구든 브라우저나 스크립트로 AI 서버에 직접 접속해서 학습된 모델에 예측 요청을 보낼 수 있어 모델이 무단으로 사용됨 | 100% | | | | | | `api_server.py:51` |
| score 계산 오류 수정 | 기존에 top-1 클래스가 benign이면 `score = 1 - top_prob`으로 계산하던 방식을, 모든 비-benign 클래스의 확률을 합산(`sum(prob for item if family != "benign")`)하는 방식으로 변경했다 | LockBit 30%·WannaCry 28%·benign 42%처럼 여러 종류의 랜섬웨어가 동시에 탐지되면 benign이 1위라는 이유로 SAFE 판정 — 실제로는 랜섬웨어 총합 58%인 위험 상황을 놓침 | 100% | | | | | | `api_server.py` — 총합 확률 합산 방식 |
| benign 판정 로직 수정 | 기존에 `"benign" in top_family.lower()`로 레이블을 결정하던 방식을 제거하고, score 임계값(≥0.70 → DANGER / ≥0.50 → WARNING / else → SAFE)만으로 레이블을 결정하도록 변경했다 | AI 모델 클래스 이름이 조금이라도 바뀌면(예: `benign_v2`) 문자열 비교가 깨져서 랜섬웨어 상황에서도 SAFE가 반환되는 오판 발생 | 100% | | | | | | `api_server.py` — score 임계값 기반 |
| requirements.txt 작성 | `코드/` 폴더에 `requirements.txt`를 생성하여 `fastapi`, `uvicorn`, `xgboost`, `lightgbm`, `pandas`, `numpy`, `pyyaml` 등 AI 서버 구동에 필요한 Python 패키지와 버전을 명시해야 한다 | 새 서버나 새 팀원 PC에서 AI 서버를 실행하려면 어떤 라이브러리가 필요한지 알 수 없어서 직접 오류를 보면서 하나씩 설치해야 하고, 버전이 달라 예측 결과가 달라질 수 있음 | | | | | | | `코드/` 폴더에 미존재 |
| config.yaml 작성 | `코드/configs/config.yaml`을 생성하여 `output_dir`(아티팩트 경로) 등 AI 서버 설정값을 파일로 관리하거나, `load_config()`가 파일 미존재 시 기본값으로 동작하도록 예외 처리를 추가해야 한다 | AI 서버 시작 시 config 파일이 없다는 `FileNotFoundError`가 발생해서 서버 자체가 켜지지 않을 수 있음 | | | | | | | `api_server.py:60` — `configs/config.yaml` 참조 중 |
| 누락 피처 처리 개선 | `_build_row_from_features()`에서 누락 피처를 `-1`로 채우는 방식을 모델 학습 시 결측값 처리 방식(0·NaN·평균값)과 동일하게 맞춰야 한다 | 모델이 훈련할 때 본 적 없는 `-1` 값이 입력으로 들어가서 예측이 틀릴 가능성이 높아짐 — 랜섬웨어를 정상으로, 정상 파일을 랜섬웨어로 판정하는 오탐·미탐 증가 | | | | | | | `features.get(c, -1)` — 학습 결측 처리 방식과 불일치 가능 |
| 모델 로드 실패 처리 | 서버 기동 시 모델 로드에 실패하면 `sys.exit(1)`로 즉시 종료하거나, `/health` 엔드포인트에서 `model is None`일 때 HTTP 503을 반환하여 로드밸런서·모니터링이 비정상 상태를 감지하도록 해야 한다 | 모델 파일이 없어도 서버가 정상적으로 켜진 것처럼 보이고, 모든 탐지 요청에 `UNKNOWN`을 반환해서 랜섬웨어 탐지 기능 전체가 아무런 경고 없이 동작하지 않는 상태가 됨 | | | | | | | 모델 없이 기동 시 모든 요청 `UNKNOWN` 반환 |
| **[배포·환경설정]** | | | | | | | | | |
| HTTPS 가이드 | `application.yml`에 Spring Boot SSL(`server.ssl.*`) 설정 방법과 Nginx 리버스 프록시(443→8080 포워딩) 설정 방법을 주석으로 작성하여 운영자가 배포 시 참고할 수 있도록 했다 | HTTP로 운영하면 관리자 비밀번호, 세션 쿠키, 탐지 알림 데이터가 네트워크에서 평문으로 오가서 같은 Wi-Fi에 있는 누구든 패킷 캡처로 탈취 가능 | 100% | | | | | | `application.yml` 주석 추가 완료 |
| 일반 사용자 인증 구현 | 현재 `/notifications`, `/logs`, `/settings` 등 일반 사용자 페이지에 로그인이 전혀 없다. 사용자 회원가입·로그인 기능을 구현하고 인증된 사용자만 대시보드에 접근할 수 있도록 해야 한다 | URL만 알면 인터넷의 누구든 탐지 로그·알림·설정에 접근하거나 감시 폴더를 변경할 수 있음 — 외부 공개 서비스에서 가장 치명적인 보안 구멍 | | | | | | | 일반 사용자 페이지 전체 인증 없음 |
| HTTPS 실제 적용 | `application.yml`에 가이드 주석만 있고 실제 SSL 인증서 적용은 안 된 상태다. Let's Encrypt 인증서를 발급받아 Nginx 리버스 프록시 또는 Spring Boot SSL로 실제 HTTPS를 적용해야 한다 | HTTP로 운영하면 로그인 비밀번호, 세션 쿠키, 탐지 데이터가 네트워크에서 평문으로 오가서 같은 네트워크의 누구든 패킷 캡처로 탈취 가능 | | | | | | | Let's Encrypt + Nginx 권장 |
| CORS dev 주소 제거 | 배포 시 반드시 `CORS_ALLOWED_ORIGINS` 환경변수에 실제 운영 도메인만 지정해야 한다. 현재 기본값에 `localhost:5173`(Vite dev 서버)이 포함되어 있어 환경변수를 설정하지 않으면 dev 주소가 허용됨 | 환경변수를 설정하지 않고 배포하면 개발자 PC의 Vite 서버에서 운영 백엔드 API를 직접 호출할 수 있게 되어 개발 코드가 운영 데이터에 접근하는 비정상 상황이 발생 | | | | | | | `allowed-origins` 기본값에 `localhost:5173` 포함 |
| Docker/docker-compose 구성 | Spring Boot·FastAPI·React 각각의 `Dockerfile`을 작성하고 `docker-compose.yml`로 묶어 `docker-compose up` 한 명령으로 전체 시스템이 기동되도록 구성해야 한다 | 새 서버에 배포하거나 다른 팀원이 실행하려면 Java·Python·Node 환경을 각각 직접 설치하고 터미널 3개를 열어 순서에 맞게 수동으로 실행해야 함 | | | | | | | `Dockerfile` × 3 및 `docker-compose.yml` 미존재 |
| **총 진행상황(%)** | | | **53%** | | | | | | |
