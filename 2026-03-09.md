# 2026-03-09 작업 일지

## 1. P0: 관리자 비밀번호 BCrypt 해싱

평문 비밀번호 비교 방식을 BCrypt 검증으로 전환.

| 파일 | 변경 내용 |
|------|----------|
| `build.gradle` | `spring-security-crypto` 의존성 추가 |
| `admin/AdminAuthService.java` | `BCryptPasswordEncoder` 도입. `ADMIN_PASSWORD`가 `$2a$`/`$2b$`로 시작하면 BCrypt 검증, 아니면 평문 비교 (기존 배포 호환 유지) |
| `src/main/resources/application.yml` | BCrypt 해시 설정 방법 주석 추가 |
| `start.sh` | `JAVA_HOME=Java 17` 명시 — Gradle 8.x가 Java 25 미지원, 시스템 기본 Java가 25이므로 명시 필요 |

### BCrypt 해시 교체 방법
```java
// Java 코드로 해시 생성
new BCryptPasswordEncoder().encode("yourPassword")
```
생성된 해시를 `.env`의 `ADMIN_PASSWORD` 값으로 교체하면 즉시 적용됨.

## 2. P1-1: AI 서버 RestTemplate 타임아웃 설정

| 파일 | 변경 내용 |
|------|----------|
| `ai/AiService.java` | `new RestTemplate()` → `@PostConstruct`에서 `SimpleClientHttpRequestFactory`로 초기화. connectTimeout / readTimeout `@Value` 주입 |
| `application.yml` | `ai.connect-timeout-ms: ${AI_CONNECT_TIMEOUT_MS:5000}`, `ai.read-timeout-ms: ${AI_READ_TIMEOUT_MS:15000}` 추가 |

## 3. P1-2: CORS 환경변수화

| 파일 | 변경 내용 |
|------|----------|
| `common/config/WebConfig.java` | `@Value("${cors.allowed-origins}")` 주입으로 origins 중앙화 |
| `application.yml` | `cors.allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,...}` 추가 |
| 컨트롤러 8개 | `@CrossOrigin` 어노테이션 및 관련 import 전부 제거 |

## 4. P1-3: HTTPS 설정 가이드

`application.yml`에 Spring Boot SSL 및 Nginx 리버스 프록시 설정 방법 주석으로 추가.

## 5. P2-2: 프론트엔드 alert() → Toast 교체

자체 Toast 컴포넌트(`Toast.jsx`) + `ToastProvider` 구현. 전 페이지 `alert()` 제거.

| 파일 | 변경 내용 |
|------|----------|
| `components/common/Toast.jsx` | ToastProvider, useToast 훅, 4가지 타입(error/success/info/warn), 3s 자동 닫힘 |
| `App.js` | `<ToastProvider>` 로 전체 앱 감싸기 |
| `LogsPage.jsx` | 3개 alert → toast |
| `MainBoardPage.jsx` | 5개 alert → toast, useToast import 추가 |
| `AdminAlertPage.jsx` | 1개 alert → toast, useToast import 추가 |
| (이전 세션) `AdminNoticePage`, `AdminFeedbackPage`, `AdminMainPage`, `AdminLogPage`, `SettingFeedbackPage`, `SettingUpdatePage`, `SettingExceptionsPage` | 각 alert → toast |

## 6. P2-1: 알림 통계 API 구현

백엔드 `/notifications/stats` 엔드포인트 구현 + 프론트엔드 연동.

| 파일 | 변경 내용 |
|------|----------|
| `alerts/NotificationRepository.java` | `countByLabelForOwner()` — ai_label별 GROUP BY 쿼리 추가 |
| `alerts/NotificationService.java` | `getStats()` — label카운트 집계 후 `{counter:{total,DANGER,WARNING,SAFE,UNKNOWN}}` 반환 |
| `alerts/NotificationController.java` | `GET /notifications/stats` 엔드포인트 추가 |
| `api/NotificationsApi.js` | `fetchAlertStats()` mock 제거 → 실제 `/notifications/stats` 호출로 교체 |

## 7. P2-3: SQLite WAL 모드, P2-4: logback 롤링 (이전 세션 완료)

## 8. P3: Spring Security + 세션 타임아웃 + Actuator 설정

| 파일 | 변경 내용 |
|------|----------|
| `build.gradle` | `spring-security-crypto` → `spring-boot-starter-security` 교체 |
| `common/config/SecurityConfig.java` | 신규. `anyRequest().permitAll()`, CSRF disable, 보안 헤더(X-Frame-Options DENY, X-Content-Type-Options nosniff) 활성화. admin 인증은 기존 AdminAuthInterceptor 유지 |
| `application.yml` | `server.servlet.session.timeout: 30m` 추가 |
| `application.yml` | `management.endpoints.web.exposure.include: health`, `show-details: never` 추가 |

## 빌드 결과

```
P0 (BCrypt): JAVA_HOME=Java 17 ./gradlew build -x test --no-daemon → BUILD SUCCESSFUL
P1 (타임아웃 + CORS): ./gradlew build -x test --no-daemon → BUILD SUCCESSFUL
P2 (WAL + logback + Toast + 통계 API): ./gradlew build -x test --no-daemon → BUILD SUCCESSFUL
P3 (Spring Security + 세션 타임아웃 + Actuator): ./gradlew build -x test --no-daemon → BUILD SUCCESSFUL
```

## 9. window.confirm() → 커스텀 ConfirmModal 교체

| 파일 | 변경 내용 |
|------|----------|
| `components/common/ConfirmModal.jsx` | 신규. `ConfirmProvider` + `useConfirm()` 훅. `confirm(message)` → `Promise<boolean>` 반환. 다크 오버레이 + 취소/확인 버튼 |
| `App.js` | `<ConfirmProvider>` 추가 (`<ToastProvider>` 내부) |
| `LogsPage.jsx` | 2개 window.confirm → await confirm |
| `AdminAlertPage.jsx` | 1개 window.confirm → await confirm |
| `AdminFeedbackPage.jsx` | 1개 window.confirm → await confirm |
| `AdminLogPage.jsx` | 2개 window.confirm → await confirm |
| `AdminNoticePage.jsx` | 1개 window.confirm → await confirm |
| `SettingResetPage.jsx` | 1개 window.confirm → await confirm |
| `SettingExceptionsPage.jsx` | 1개 window.confirm → await confirm (handleRemove async로 변환) |
